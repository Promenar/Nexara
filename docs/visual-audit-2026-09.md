# Nexara 全页面视觉审计报告（2026-09-23）

- 执行方式：定时任务 11:40 触发，按已批准计划（`.agent/tmp-agent-reports/visual-audit-plan.md`）执行 Phase 0–7
- 设备：Pixel 7 AVD `Nexara_Audit_API_35`，1080×2400 @420dpi（1dp = 2.625px），font_scale 1.0，状态栏 Demo Mode
- 真实服务：本地聚合网关（chat/流式/工具调用/embeddings/rerank 全部真实调用，仅使用 5 个免费模型 ID）+ SearXNG 自建实例（真实联网搜索）
- 原始资产：`native-ui/build/audit/`（96 张截图 PNG、11 份 UI bounds XML、3 份测量 JSON）——构建产物，不入库
- 四层方法：L1 代码 token 静态扫描 / L2 UI bounds 结构度量 / L3 像素采样与几何测量 / L4 视觉子代理逐张批阅

---

## 一、发现矩阵

### P0（渲染/功能阻断）

| # | 问题 | 证据 | 层 |
|---|---|---|---|
| 1 | **ECharts 图表中文轴标签全部乱码**（UTF-8 被 Latin-1 误读，`è®¾å¤`式 mojibake） | `B03_rich_reply_dark.png` | L4 |
| 2 | **Mermaid 节点/边标签中文全部乱码**（同上编码错乱） | `B04`/`B05_rich_table_latex_dark.png` | L4 |
| 3 | **行内 LaTeX 未渲染**：`$x = \frac{-b \pm \sqrt{b^2-4ac}}{2a}$` 原样显示（单 `$` 定界符不支持） | `B05` | L4 |
| 4 | **image_generation 工具未暴露给模型**：工具设置中已启用、图像模型已配置，但模型自述工具集中无此工具（`Calculator/Web search/Web fetch/File tools`），生图退化为模型手写 SVG | 会话 `Generate an ima...` 12:39/12:41 回复 | L4+主控 |

### P1（明显可用性/一致性缺陷）

| # | 问题 | 证据 | 层 |
|---|---|---|---|
| 5 | 富文本悬浮操作按钮遮挡内容：分别压住 ECharts 柱体、Mermaid 菱形中心、表格数字 | `B03/B04/B05/B06` | L4 |
| 6 | Kotlin 代码块行号与内框错位，闭合 `}` 渲染逃逸到内框外 | `B05/B06` | L4 |
| 7 | 代码块括号/花括号近黑深灰，暗底下几乎不可见 | `B05/B06` | L4 |
| 8 | 多模态用户气泡中图片被压成细条（高度塌陷不可辨认） | `E04_generation_failure_dark.png` | L4 |
| 9 | **12 处可点击元素小于 48dp 触控标准**：代码块工具按钮 84px(32dp)，Agent 编辑色板 116px(44dp) | `layer2_bounds.json` | L2 |
| 10 | 会话删除在特定状态下报「会话删除失败，数据已保留，请重试」（含失败生成态的会话稳定复现；普通会话亦出现） | `E05`（确认对话框）+ 删除后列表仍完整 | 主控 |

### P2（打磨级）

| # | 问题 | 证据 | 层 |
|---|---|---|---|
| 11 | **代码 token 违规 1519 处 / 78 文件**：dp=4(145)、20(83)、10(41)、14(24)、40(19)、3(16) 均不在 12 档标尺 [0,1,3,4,6,8,12,16,18,24,32,48]；`RoundedCornerShape` 字面量 44 处 | `layer1_token_scan.json` | L1 |
| 12 | sp 标尺 13/14/15 三个近似正文字号并存，建议归并 | `layer1_token_scan.json` | L1 |
| 13 | 设置分组标题颜色不一致：「通用」灰白 vs「AI 与模型」等紫色强调 | `B20` | L4 |
| 14 | 设置列表底部「工具与数据」标题被悬浮底栏裁切 | `B20` | L4 |
| 15 | 模型卡片能力字段直接显示原始值 `unknown`（bge-m3/bge-reranker 修复后已变「嵌入/重排」，但同步目录仍会复现） | `B24` | L4 |
| 16 | 检索结果摘要裸露 Markdown 井号标记 | `C05` | L4 |
| 17 | 文档编辑器左有边距右为零，长行右缘硬切 | `C09` | L4 |
| 18 | 模型署名行空间充足时仍提前省略号截断 | `E04` | L4 |
| 19 | Provider 连接失败仅显示「失败」二字，无 HTTP 状态/原因 | `A04` | 主控 |

### 层3 基线（无违规，供后续回归对照）

- 暗色主 app 背景 `#131315`（统一）；onboarding 流程底色 `#19191E`（偏蓝，若非有意渐变需归一，采样点受渐变影响待复核）；弹层截图因 scrim 变暗属预期。
- 亮色全套背景 `#FBF8FB`（14 张完全一致）。
- 主按钮几何 176×48dp、圆角 ≈16–18dp（与 token 16dp 吻合，像素量化 ±1px）；Agent 头像 82dp。

---

## 二、覆盖清单

**已覆盖（暗色 70+ 张）**：Onboarding 6 步全流程（含连接失败/成功、通知权限两级对话框、StartupGate 恢复门）、聊天（空态、纯文本、流式「思考中」、推理折叠+展开、web_search 工具管线、富文本 6 种内容块、生图请求、多模态附图+识别、生成失败红条、9 类弹层菜单）、RAG（空态、SAF 导入、就绪 chip、真实检索命中、文档编辑器编辑+预览、知识图谱空态、记忆空态）、设置（主页×2、默认模型未设置/已配置、模型目录、模型编辑弹层、检索设置×2、记忆设置、技能页、SearXNG 配置、本地模型、语言对话框、主题页、用量（真实 15.3K token）、备份）、模型同步成功横幅。

**亮色回归（12 张）**：主题页、设置主页、Hub、会话列表、聊天、知识库、文档预览、默认模型、提供商列表/模型、用量。背景一致性经 L3 像素验证。

**空态（5 种）**：知识库空、图像模型无可用、本地模型未配置、知识图谱无数据、记忆空。

**未覆盖（含原因）**：生图真实结果（P0-4 工具未暴露）、知识图谱有节点态（抽取未随导入触发）、分享导入弹层（SEND intent 被 app 静默处理未出弹层）、头像裁剪、创建助手颜色选择器、同步失败态（坏 Provider 未保存成功，失败语义已由 A04/E04 覆盖）、关于页（误触外部链接，已排除样本 D06）、亮色下 WebView 图表复核（乱码为 charset 解码问题，与主题无关，推断成立未实证）。

---

## 三、修复验收（2026-09-23 第二轮，针对 commit `834fd4e0`）

重建装机后按原场景重拍回归（`shots/R01–R06`），并核对了修复 diff 与新增单测（`DefaultSessionToolResolverTest`、`RagSearchResultsTest` 均通过）。

| 发现 | 验收结果 | 实证 |
|---|---|---|
| P0-1 ECharts 中文乱码 | ✅ 已修复 | 轴标签「苹果/香蕉/橙子」正常；charset 改为 Uint8Array+TextDecoder，方案正确 |
| P0-2 Mermaid 中文乱码 | ✅ 已修复 | 「开始/是否完成?/是/否/结束」全部正常 |
| P0-3 行内 LaTeX | ❌ 未修复 | 列表项内 `$x = \frac{...}$` 仍原样显示；新正则在真实消息上下文未生效 |
| P0-4 生图工具 | ⚠️ 半通过 | **注入已通**：模型可发起 `generate_image` 并触发「需要审批」卡片（新审批流工作正常）；**执行层新断点**：三种参数自适应重试全部「工具执行失败，请检查工具参数与配置后重试」 |
| P1-5 悬浮按钮遮挡 | ⚠️ 部分 | ECharts 原生 toolbox 已关闭（遮挡源消除）；聊天「回到底部」FAB 仍压表格/Mermaid 节点/dataZoom |
| P1-6 代码块错位 | ✅ 已修复 | 行号对齐、闭合括号回到框内 |
| P1-7 括号对比度 | ✅ 已修复 | 暗底/亮底均可见（亮色会话复核） |
| P1-9 触控目标 | ✅ 代码层通过 | Mermaid 按钮 48dp、色板扩大已在 diff；未逐一实测像素 |
| P1-10 会话删除 | ❌ 未修复 | 「会话删除失败，数据已保留，请重试。」稳定复现；commit 所修为「物理工作区缺失」子场景，与本复现场景不符 |
| P2-13 分组标题颜色 | ✅ 已修复 | 通用/AI 与模型/知识与检索 统一紫色 |
| P2-14 底栏裁切 | ✅ 目测通过 | 现为正常滚动截断 |
| P2-15/16/17/18/19 | 未复核 | 本轮未逐一触达，留待下轮 |

**结论：部分通过。** P0 中 2 项修复确认、1 项未生效、1 项半通过（暴露修复但执行断）；P1 中 3 项确认、2 项未决。

### 第三轮修复与验收（2026-09-24，用户裁决项 + 验收未过项，主控直修）

| 项 | 结果 | 实证 |
|---|---|---|
| P0-3 行内 LaTeX | ✅ 已修复并实证 | 根因：`repairCompressedMarkdownBoundaries` 的内联列表符号规则把公式内 " - " 折为换行，`$...$` 跨行失配。修复：LaTeX span 纳入压缩修复预保护。设备实测求根公式渲染为 KaTeX 图形。逐级管线诊断测试固化（`InlineLatexPipelineBreakTest`） |
| P1-10 会话删除 | ✅ 已修复并实证 | 根因：DB 工作区路径以 `/data/data/<pkg>` 别名落库，与运行时 `/data/user/0/<pkg>` 父目录做字符串前缀比对误判越界（SecurityException）。修复：resolver 与 journal 全部路径比对 canonical 归一。设备实测删除成功（无失败文案，DB 会话数 8→7，workspace 目录清理） |
| P0-4 生图执行 | ⚠️ 网关侧 | 注入与审批流正常；执行返回 503/model_not_found「No available channel for model sensenova-u1.5-lite」——网关通道不可用，非应用缺陷。应用侧已做两项增强：请求参数最小化（去掉网关不接受的 quality/response_format 默认注入）+ 工具错误详情透传（原先统一文案致模型盲试） |
| P1-5 悬浮按钮遮挡 | ⚠️ 部分 | ECharts 原生 toolbox 已消除；「回到底部」FAB 在长会话仍可能与内容视觉重叠（低优先） |
| 管线气泡样式统一（用户验收意见） | ✅ | 工具气泡改无边框 `surfaceContainerLow` 全宽（与思考框一致）；步骤间 6dp 间距；引导线与死代码 PipelineConnector 删除 |
| 品牌 Logo 底座形态打磨（用户第二轮反馈） | ✅ | 方形大底座改圆形小底座（选择器 32dp/Provider 页 36dp），贴合全站圆形视觉语言 |
| 管线气泡字号/内边距/箭头统一（用户第二轮意见） | ✅ | 工具气泡标题升为 `labelLarge`（与思考框同字号）、水平内边距对齐 `Large`、状态图标与展开箭头统一 16dp |
| Markdown 特殊块与标题间距统一（用户第二轮意见） | ✅ | 分段渲染列增加 `spacedBy(8dp/compact 4dp)` 呼吸间距，标题→GFM 块与标题→图表间隔一致 |
| 上下文胶囊高度与浮窗圆角（用户第二轮意见） | ✅ | 模型/占用/任务三胶囊高度 48→36dp（用户裁决：展示胶囊可接受低于触控标准）；上下文用量浮窗改 `shapes.large` 16dp 圆角 |
| P2-15/16/17/18/19 | ✅/未复核 | 由 Gemini 第一轮修复覆盖（unknown 标签、RAG 标题清洗、编辑器边距、Badge 展开、Provider 探测错误），本轮未逐项复拍 |

**本轮验证**：新增/更新单测全绿（估算器、格式化、分段管线诊断、会话删除协调器）；全量 JVM 单测 0 失败；设备端删除/LaTeX/管线样式/上下文圈逐项目视确认。

## 四、修复优先级建议

1. **P0-1/2**：RichContentWebView/ECharts/Mermaid 管道 charset 配置（疑似 WebView 加载未声明 UTF-8 或内容被按 Latin-1 解码）——一处修复惠及两类图表。
2. **P0-3**：LaTeX 渲染器支持单 `$` 行内定界（InlineLatexSpan 已存在，接匹配规则）。
3. **P0-4**：排查 image_generation 注入条件（`ToolOrchestrationPlugin`/`ContextBuilder`），确认工具注册与会话建立的时序耦合。
4. **P1-5/6/7**：富文本悬浮按钮层级落点；代码块行号框与高亮框对齐；代码 token 配色提亮括号色。
5. **P1-9**：代码块工具按钮与色板补足 48dp 触控（L2 有完整坐标清单）。
6. **P2**：token 治理专项——先决策是否把 4/10/20/40dp 纳入标尺，再批量清理 1519 处字面量；其余逐条打磨。
7. **功能疑点**：会话删除失败（`SessionDeletionUiAction` 为薄封装，根因在领域层删除事务，建议查 `SessionRepository` 删除路径对失败生成态的处理）。

## 五、修复后回归重拍

每场景的导航与操作步骤记录于会话 manifest（截图文件名即场景 ID，命名规则 `域+序号+场景+主题`）。修复后按名单重拍对应截图与 bounds 做像素 diff 即可回归；控制变量（font_scale/Demo Mode/分辨率）见 `layer3_pixels.json` 同目录计划文件。

---

## 六、补充：用户报告体验缺陷诊断（2026-09-23，验收后追加）

### 缺陷 A：会话上下文占用圈恒显「0K / 1000K」

> **修复记录（2026-09-23 第三轮）**：已实施——①`formatTokenCount` 自适应显示（<1K 原始数、≥999.5K 进 M）+ contextLength≤0 兜底 128K；②`estimateTokens` CJK 感知估算。设备实测：占用圈由「0K / 1000K」变为「412 / 1M」。新增单测 `PostProcessorEstimateTokensTest`、`ChatViewModelFormatTokenCountTest` 全绿，全量 JVM 单测 0 失败。

- **取数链**：`ChatScreen.kt:1003` 显示 `state.used / 1000`；`used` 由 `ChatViewModel.updateTokenIndicator`（`ChatViewModel.kt:1951`）计算 = 系统提示 + 摘要 + 最近 10 条消息 + RAG，全部按 `PostProcessor.estimateTokens = 字符数/4` 估算。
- **实测根因（DB 佐证）**：最富会话消息共 3024 字符 ≈ 756 token → `756/1000 = 0`（整数除法截断）→ 恒显 "0K"。分母为 1M 上下文时，进度环占比 0.08%，环与数字在该量级**均无有效分辨率**——用户感知「完全没纳入统计」成立。
- **精度复核（用户质询后追加）**：对纯英文内容估算器本身自洽——生图会话 DB 逐条对账（user 85 / assistant 1207 / tool 21×3 字符）与弹层「活跃对话 337」完全吻合，且英文 chars/4 ≈ 真实 BPE 量级。**真正的失真是统计范围**：①工具 schema 定义（1–3K token）未计入；②多模态图片 token（数百/张）未计入；③`字符数/4` 对中文低估约 2–3 倍（中文 BPE ≈ 1.5–1.8 字符/token），中文会话活跃 token 被显著低报；④系统指令仅 39 是默认提示词近乎为空所致。
- **次生缺陷**：sensenova 模型目录缺 contextLength 时 `max = 0`，显示「0K / 0K」——`ChatViewModel.kt:1997-1999` 的 `?: 128000` 兜底只兜 null 不兜 0。
- **修复建议**：①显示改自适应单位（<1K 显示原始 token 数或一位小数，≥1K 才用 K）；②max 兜底把 `<=0` 一并视为未提供；③低占比下指示器给出最小可视进度。

### 缺陷 B：带附件发送时输入框卡「待发」数秒

> **修复记录（2026-09-23 第三轮）**：已实施降采样修复——`uriToDataUrl` 两段式解码 + `inSampleSize`（最长边 1568px）+ JPEG 85%，单测全绿；设备端附件流验证受自动化环境限制未走通，待人工确认（步骤：会话中 + → 图片 → 选大图 → 发送，观察输入框清空耗时）。

- **链路**：`sendMessage`（`ChatViewModel.kt:643`）置 `GenerationStatus.UPLOADING` 后，在发送准备锁内同步执行 `uriToDataUrl`（`ChatViewModel.kt:867`）：`readBytes()` 全量读图 + `Base64.encodeToString`，**无降采样/压缩**。相机原图 3–8MB 时读取+编码即秒级阻塞，且发生在输入框清空之前——即用户看到的「卡在待发」。
- **用户猜测的 RAG 检索**发生在消息入队后的生成管线（ContextBuilder），此时输入框已让位，不是本阻塞的成因。
- **附加缺陷**：base64 data URL 直接写入 Room 消息行（`userImages`），库体积随附件数膨胀、历史加载变慢。
- **修复建议**：发送前按视觉模型输入上限降采样再编码（或先清输入框、转换移入后台服务并给附件级进度）；长期应避免把 data URL 整块入库。

---

*审计执行：ZCode 定时会话（GLM-5.3-Flash 主控 + k3-256k 视觉子代理）；密钥与计费纪律：全程仅 5 个免费模型 ID，密钥未入库未落报告，审计结束即删除。*
