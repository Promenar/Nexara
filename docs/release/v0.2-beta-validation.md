# Nexara v0.2-beta 发行验证记录

> 状态：**NO-GO / PENDING**
> 更新日期：2026-07-15
> 分支：`codex/v0.2-beta`
> 当前已推送基线提交：`f8e52a79dd72e52ffe79a91201907f23f5b65bbb`（本轮 CI 修复、RAG/分享验真与截图证据加固仍在 dirty tree，尚未形成下一提交）

本文件是发行事实账本。只有本轮实际执行并可复核的结果才能标为 `PASS`；源码存在、测试文件编译、旧构建截图、计划或子 Agent 声明不能代替最终运行证据。

状态含义：

- `PASS`：当前工作树或明确记录的基线已经执行并取得可复核证据；
- `PENDING`：尚未执行、证据不完整或需要最终集成后重跑；
- `FAIL`：已执行但不满足门禁，必须修复后重跑；
- `N/A`：经产品范围明确排除，并记录理由。

## 1. 当前放行结论

| 门禁 | 状态 | 结论 |
|---|---|---|
| P0/P1 安全、数据与核心业务基线 | PASS | 2026-07-13 交接记录：1232 JVM、28 Android、Lint、Debug APK，以及备份/恢复多阶段流程通过；该证据属于 P0/P1 基线，不替代当前 dirty tree 全量回归。 |
| P2 后台生成与 UI 专项实现 | PASS | 生成、通知、RAG、Prompt、资源树、设置、Provider、备份恢复与文档解析已完成确定性 JVM/截图/设备回归；API 31 minimum、API 35/36 full 均通过。 |
| P3 签名 R8 APK 与发行工程 | PENDING | API 35/36 的 release-equivalent minifiedTest 冷启动与 PDF/DOCX 分享导入已通过，API 36 进一步覆盖 TXT 导入、索引失败和再次重试；workflow fail-closed 校验 16/16 且独立复审 GO，但尚无最终签名 R8 APK、release mapping、签名包冷安装和全绿远端 workflow 证据。 |
| 商业级 UI/设备矩阵 | PENDING | 当前 36/36 Screenshot validation、API 31/35/36 欢迎布局、自适应导航、IME 5/5 与无障碍 smoke 已通过；36 张基线已逐张人工审阅。分享表单的 DOCX 长 MIME 窄屏换行缺陷已改为紧凑标签，四动作区改为可换行布局，并在 API 36 的常规画面与 2.0x 字体 Compose 测试中复验。TalkBack 真实绑定下的自动语义门禁已通过，人工听觉与完整焦点遍历仍待最终验收。 |
| GitHub tag 与 Release | PENDING | 尚未创建最终 tag 或上传 APK；当前结论为 NO-GO。 |

## 2. 已确证的专项测试

下列结果来自 2026-07-13 至 2026-07-15 的本轮报告或主控独立复跑；最终发行仍须完成签名与远端门禁。

| 范围 | 命令/证据 | 结果 |
|---|---|---|
| 生成取消、Runner、Runtime、路由与流式背压 | 8 类定向 JVM 套件 | PASS：62 tests，0 failed，0 skipped；最终取消契约 9/9。 |
| Anthropic/OpenAI Responses 错误分类 | 两协议定向与组合回归 | PASS：定向 20/20；组合 58 tests，0 failed，4 个既有真实网络配置门禁 skip。 |
| RAG 移动、失败目标、重试与通知 | `RagViewModelTest`、`IndexingNoticeTest`、`RagHomeInteractionTest` | PASS：31 tests，0 failed/skipped/errors。 |
| 统一 Prompt 编辑器 | `UnifiedPromptEditorTest` + Screenshot validation + device E2E | PASS：JVM 5/5，截图基线已纳入 36/36 全量通过，AndroidTest 编译与设备主流程通过。 |
| Release 日志源码契约 | `ReleaseLoggingSourceContractTest`、`PostProcessorTest` + minified 黑盒日志 | PASS：3/3 + 3/3；API 35/36 minifiedTest 冷启动与文档分享导入无 crash/ANR，最终签名 APK logcat 仍待验。 |
| APK 验证器 | `python3 -m unittest scripts/tests/test_verify_release_apk.py` | PASS：23 tests，OK；已兼容 Android 16 `aapt` 与 Build Tools 37 `apksigner` 输出，重复包身份行或现代格式缺失 signer 计数时均 fail-closed；并对 17,921,194-byte minifiedTest APK 完成包身份、单签名、敏感内容、GGUF/本地推理制品与 SHA-256 全扫描。最终发行证书 APK 仍 PENDING。 |
| Metro 开发者 TUI | `node --test scripts/tests/nexara-metro-tui.test.js` | PASS：49 tests，0 fail；真实 adb 长时间运行不属于发行 APK 门禁。 |
| Release workflow 可靠性契约 | Python/Shell 契约、`bash -n`、Ruby YAML parse、独立复审 | PASS：fail-closed 动态正反例 16/16，全部 Python 合约 58/58、Metro TUI 49/49，设备脚本契约/语法与 6-job YAML 解析通过；独立复审 GO。`actionlint`、`shellcheck` 本机不可用，远端 run PENDING。 |
| Room 关闭重开与分享索引重试 | `VectorizationQueueRoomTest`、`ShareImportMimeTypeTest`、API 36 Compose、release-equivalent minified 黑盒 | PASS：文件型 Room close/reopen 后恢复失败任务并复用同一 task id 重置为 pending；已知 MIME 紧凑显示单测通过；API 36 Compose 2/2，含 2.0x 字体四动作显示、触控高度与边界断言；混淆包 TXT 首次失败与再次重试均可观察且无 crash/ANR。 |
| Android 截图证据链 | `test-normalize-android-screencap-png.py`、minified 黑盒 8 张 `screen-*.png`、真实异常 PNG 像素比对 | PASS：标准库工具覆盖 PNG CRC、filter 0–4、坏输入拒绝、同路径原子替换；历史异常图归一化前后 1080×2400 RGB 逐像素一致且 alpha=255。最终 API 36 黑盒 8/8 PNG 均为 alpha=255、无 raw/current/previous/tmp 残留；DOCX indexed 与 TXT refailed 经无 alpha JPEG 视觉副本复核，画面、状态和动作完整。 |
| 首次远端 Android CI | [run 29389720531](https://github.com/Promenar/Nexara/actions/runs/29389720531) | FAIL（已定位并本地修复）：JVM/Lint/构建成功，API 31 成功；API 35 因权限弹窗只搜索活动根而失败，API 36 因 onboarding 过早访问 ProviderManager 而失败。API 36 两条修复均已本地精确复验，第二次 push run 待回填。 |

## 3. 最终集成质量门禁

| 项目 | 状态 | 待回填证据 |
|---|---|---|
| clean 后全量 JVM | PASS | JDK 21 `:app:testDebugUnitTest --rerun-tasks`：1632 tests，0 failure/error，14 skipped；随后 JDK 17-like clean 发行门禁再次通过。 |
| Android Lint | PASS | `:app:lintDebug` 通过：0 Error/Fatal；412 条 warning 均未阻断构建，最终报告位于 `native-ui/app/build/reports/`。 |
| Screenshot validation | PASS | `:app:validateDebugScreenshotTest`：36/36 通过；新增 4 张横屏基线，资源列表改为固定时钟；文档冲突大字体状态在优先保障两个 48dp 操作后更新 1 张预期基线，并再次全量通过。 |
| Android instrumentation | PENDING | 既有 API 31 minimum、API 35 full、API 36 full、三版本 IME 5/5 与 API 36 TalkBack/DocEditor 均已通过；首次远端 push run 中 API 31 继续通过，但 API 35/36 暴露两项 CI 环境竞态。修复后 API 36 onboarding 连续 3 次通过、通知拒绝精确用例通过，API 36 分享表单 2/2 通过；API 35 的最终证明等待第二轮远端矩阵。 |
| Debug APK | PASS | JDK 17-like clean 门禁中的 `:app:assembleDebug` 通过。 |
| Release APK | PENDING | 当前集成工作树 `assembleRelease` 结果、文件名、原始字节数。 |

建议最终命令（以实际 Gradle task 为准）：

```bash
cd native-ui
./gradlew --no-daemon clean \
  :app:testDebugUnitTest \
  :app:lintDebug \
  :app:validateDebugScreenshotTest \
  :app:assembleDebug \
  :app:assembleRelease
```

## 4. 签名、R8 与 APK 安全

| 项目 | 状态 | 待回填证据 |
|---|---|---|
| 稳定发行证书 | PENDING | 只记录证书 SHA-256 指纹与有效期，不记录 keystore 路径、别名或密码。 |
| R8 mapping | PENDING | `mapping.txt` 非空；记录内部保管路径/Release artifact，不公开敏感映射时说明策略。 |
| R8 seeds/usage/configuration | PENDING | 存在则记录；缺失需说明 AGP 行为。 |
| APK 包身份 | PENDING | `com.promenar.nexara.native`、versionCode 2、versionName `0.2-beta`。 |
| 单一签名者/证书匹配 | PENDING | `apksigner verify --verbose --print-certs` 与登记指纹匹配。 |
| ZIP/体积/敏感内容扫描 | PENDING | `verify-release-apk.py` 真实 APK 结果；APK 必须不超过 50 MiB。 |
| zipalign | PENDING | `zipalign -c -P 16 4`。 |
| SHA-256 | PENDING | 最终 digest 与 `.sha256` 文件内容；下游再次 `sha256sum -c`。 |
| Release logcat | PENDING | 主流程无 Prompt、响应、Authorization、Key、SQL 参数或完整堆栈。 |
| GGUF/llama/ggml 排除 | PENDING | 真实 APK 扫描结果。 |

## 5. 冷安装与 minified 业务验真

| 场景 | API 35 | API 36 | 证据要求 |
|---|---|---|---|
| 卸载旧包、侧载签名 APK、冷启动、无 crash/ANR | PENDING | PENDING | 安装/版本/launcher/进程与 crash buffer。 |
| Room 新建、强停、重启读取 | PENDING | PENDING | minified APK 实际数据。 |
| 中英文 onboarding 至首聊 | PENDING | PENDING | 截图 + UI tree/测试报告。 |
| Provider 表单与模型选择 | PENDING | PENDING | 不使用真实 Key 的黑盒流程。 |
| 无 Key 备份 round-trip | PENDING | PENDING | 导出、强停、恢复、no-replay。 |
| 含 Key 加密备份 round-trip | PENDING | PENDING | 仅用脱敏测试凭据，不在日志/截图暴露内容。 |
| DOCX/PDF 解析 | PASS | PASS | API 35/36 `minifiedTest` APK 使用真实 PDF/DOCX fixture 完成分享导入，覆盖 R8/反射边界；证据位于对应 `artifacts/android-minified-blackbox-*` 目录。 |
| RAG 导入/索引/重试 | PENDING | PASS | API 36 release-equivalent minifiedTest 使用真实 TXT fixture 完成导入，观察到索引失败、启用 Retry、点击后再次失败并可再次重试；API 35 同场景待远端/本地补证。 |
| 后台/锁屏/旋转/停止/返回 | PENDING | PENDING | 当前任务持续、终态唯一、通知返回准确会话。 |

当前 workflow 的签名 APK smoke 只覆盖校验、冷安装、启动、前台存活与 crash/ANR；在 minified 关键业务流程补齐前，不得把该 smoke 描述为完整发行 E2E。

## 6. UI、视觉与无障碍矩阵

所有截图必须来自最终集成后的当前构建，逐张人工审阅。旧安装包或历史 `/tmp` 截图只能作线索，不能作为本版 PASS 证据。

2026-07-15 已逐张人工审阅当前 36 张截图基线：手机/平板、竖屏/横屏、中文/英文、2.0x 字体、空态/加载/错误/流式/审批/确认框等状态均未发现发行阻断级视觉缺陷；超长模型名在容器内换行，单行可编辑标题在大字体下按输入框语义横向滚动，均未造成关键操作不可达。新增横屏覆盖英文聊天空态、中文生成态、英文文档编辑脏状态和中文 Provider 长文本状态；资源列表相对时间已改为由预览固定时钟驱动，避免基线随真实时间漂移。文档冲突状态在 2.0x 字体下会优先保留复制与重载动作的完整触控目标，并把非关键状态栏让位给本地内容预览；对应预期基线已人工比较后更新。真实 IME 专项已完成，TalkBack 仍保留人工听觉与全焦点遍历门禁。

| 视口/条件 | 中文 | 英文 | 状态 |
|---|---|---|---|
| 360dp 紧凑手机 | PASS | PASS | Screenshot 基线覆盖双语紧凑布局，人工审阅通过。 |
| Pixel 7 竖屏 | PASS | PASS | 412×892 双语关键页面基线，人工审阅通过。 |
| Pixel 7 横屏 | PASS | PASS | 4 个关键横屏状态基线通过并逐张人工审阅；聊天输入区、文档保存入口、Provider 搜索及批量操作均可达。 |
| 系统字体 2.0x | PASS | PASS | 多个双语关键状态基线覆盖，人工审阅未发现关键控件不可达。 |
| 平板宽度/NavigationRail | PASS | PASS | 840×900 双语基线与 API 设备自适应导航测试通过。 |
| TalkBack 焦点、角色、动态播报 | PENDING | PENDING | API 31/35/36 无障碍 smoke 已通过；API 36 在 TalkBack 服务真实绑定时新增稳定输入名称、Prompt 编辑器名称、保存中 polite 与保存失败 assertive 语义断言，共 7/7。人工听觉与完整焦点遍历仍待补。 |
| IME 遮挡、滚动、返回、空/加载/错误态 | PASS | PASS | `ChatImeInteractionTest` 在 API 31/35/36 各 5/5；覆盖长消息尾部避让、键盘动画稳定、主动上滚不被夺回、关闭后 inset 清零与测试隔离。 |

关键页面/状态：欢迎与引导、Agent 首页、聊天空态/生成态/错误态/审批态、Provider、RAG、备份、设置、Prompt 编辑器、通知返回/停止。发现裁切、溢出、低对比、焦点陷阱、低于 48dp 的关键目标、键盘遮挡、无效按钮或不可恢复错误时，门禁为 FAIL。

## 7. 真实 API Smoke

| 项目 | 状态 | 安全要求 |
|---|---|---|
| 多模态模型 | PENDING | URL/Key 仅由主控经进程环境变量注入；不写仓库、命令回显、日志、截图或 Agent 报告。 |
| 高速文本模型 | PENDING | 只记录模型 ID、请求类型、成功/错误分类与时延区间，不记录 Prompt/响应正文。 |
| 高智力文本模型 | PENDING | 同上。 |
| 中等智力多模态模型 | PENDING | 同上。 |

真实 API 只验证协议兼容、路由、流式终态和多模态输入；Release 仍保持 HTTPS-only，不能为内网测试放宽发行网络策略。

## 8. GitHub 治理与发布

| 项目 | 状态 | 待回填证据 |
|---|---|---|
| 最终提交已推送 | PENDING | 分支、commit SHA、工作树 clean。 |
| tag provenance | PENDING | tag object、目标 commit ancestry、GitHub verification；不允许 lightweight tag。 |
| annotated 降级 | PENDING | 默认拒绝；如签名 tag 不可用，必须有用户明确授权与仓库变量记录。 |
| release environment | PENDING | 可执行的 branch/tag policy、wait gate/权限事实；不得配置单管理员自审批死锁。 |
| Android CI | FAIL / RERUN PENDING | 首次 push run `29389720531`：quality 与 API 31 通过，API 35/36 失败；根因已本地修复，第二次 push run 尚未触发。 |
| tag release workflow | PENDING | 远端 run URL、每个 job 结论。 |
| GitHub Release | PENDING | prerelease URL、APK 与 checksum 资产、远端回读哈希一致。 |

## 9. 已知限制确认

| 限制 | 状态 |
|---|---|
| 不兼容 `v0.1-beta` 数据，要求卸载旧版 | 已写入 README 与发行说明 |
| GGUF 不属于稳定能力，Release 不打包 | 已写入 README 与发行说明；真实 APK 扫描 PENDING |
| 后台生成不支持设备重启续传、多会话并行、无人值守队列或定时任务 | 已写入 README 与发行说明 |
| Release Provider/WebDAV 仅 HTTPS | 已写入 README 与发行说明 |
| 仅深色主题为正式主题 | 已写入发行说明 |

## 10. 最终签字区

- 最终结论：`NO-GO / PENDING`
- 阻断项：稳定签名身份与最终签名 R8 APK、API 35 TXT 索引重试与其余签名包业务验真、TalkBack 人工听觉/全焦点遍历、真实 API、第二轮远端 Android CI、tag 与 Release 尚未完成。
- 放行人：PENDING
- 放行时间：PENDING
- 最终 commit：PENDING
- Release URL：PENDING
- APK SHA-256：PENDING
