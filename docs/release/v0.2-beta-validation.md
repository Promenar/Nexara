# Nexara v0.2-beta 发行验证记录

> 状态：**GO**
> 更新日期：2026-07-29
> 当前工作分支：`codex/md3-redesign`
> 当前应用候选：SSH 签名 tag `v0.2-beta` 固定提交 `a7c94f03`；Release workflow run `30420324239` 全绿
> 物理真机人工验收：BETA-RISK-ACCEPTED
> GitHub 发布动作：AUTHORIZED
> 当前本地 APK 冷安装：NOT-RUN
> 最终远端 APK 冷安装：PASS（API 35/36）
> 同源历史候选冷安装：PASS

本文件是发行事实账本。只有本轮实际执行并可复核的结果才能标为 `PASS`；源码存在、测试文件编译、旧构建截图、计划或子 Agent 声明不能代替最终运行证据。

状态含义：

- `PASS`：当前工作树或明确记录的基线已经执行并取得可复核证据；
- `PENDING`：尚未执行、证据不完整或需要最终集成后重跑；
- `FAIL`：已执行但不满足门禁，必须修复后重跑；
- `N/A`：经产品范围明确排除，并记录理由。

## 1. 当前放行结论

| 门禁 | 状态 | 结论 |
|---|---|---|
| P0/P1 安全、数据与核心业务基线 | PASS | 2026-07-13 交接记录：1232 JVM、28 Android、Lint、Debug APK，以及备份/恢复多阶段流程通过；后续候选已完成 1632 JVM、36 张截图和 API 31/35/36 设备回归，该基线仍不替代最终签名包验真。 |
| P2 后台生成与 UI 专项实现 | PASS | 生成、通知、RAG、Prompt、资源树、设置、Provider、备份恢复与文档解析已完成确定性 JVM/截图/设备回归；API 31 minimum、API 35/36 full 均通过。 |
| P3 签名 R8 APK 与发行工程 | PASS | Release workflow 从签名 tag 固定的 `a7c94f03` clean 构建 18,367,648-byte 稳定证书签名 R8 APK；包身份、唯一签名者、登记证书、敏感/GGUF 扫描、checksum、16 KiB zipalign 与 API 35/36 无密钥冷安装均通过。远端下载回读 SHA-256 为 `59d641b6b8f04a15bc9f4ce064a7a116df3def4efabf8231feb408612246fe9a`。 |
| 商业级 UI/设备矩阵 | GO（beta 风险接受） | Task 14 当前候选 96/96 Screenshot validation 通过，96 张 actual 已由主控检查；API 31/35/36 冻结类集合各 101/101、0 failure/error/skip，同源 broader harness 分别 39/41/41 个显式测试且各保留 1 个设计内 phase checkpoint skip，三档 exit 0。治理 HEAD `3535735f` 的 run `29893140602` 四个 job 首轮全绿。物理真机 TalkBack 人工听觉、完整焦点遍历和更广泛 OEM/IME 体验未记为 PASS，由用户明确接受为 beta 后续验证项。 |
| GitHub tag 与 Release | PASS | SSH 签名 annotated tag `v0.2-beta` 的 GitHub verification 为 `valid`，目标提交 `a7c94f03`；[run 30420324239](https://github.com/Promenar/Nexara/actions/runs/30420324239) 全绿并公开非草稿 prerelease。APK 与 checksum 已从 Release 独立下载并通过 `shasum -a 256 -c`；既有 `v0.1-beta` 按用户最终指示保留。 |

## 2. 已确证的专项测试

下列结果来自 2026-07-13 至 2026-07-22 的报告或主控独立复跑；当前本地 APK 与同源历史冷安装证据分开记账，物理真机人工项按上述 beta 风险接受处理。

| 范围 | 命令/证据 | 结果 |
|---|---|---|
| 生成取消、Runner、Runtime、路由与流式背压 | 8 类定向 JVM 套件 | PASS：62 tests，0 failed，0 skipped；最终取消契约 9/9。 |
| Anthropic/OpenAI Responses 错误分类 | 两协议定向与组合回归 | PASS：定向 20/20；组合 58 tests，0 failed，4 个既有真实网络配置门禁 skip。 |
| RAG 移动、失败目标、重试与通知 | `RagViewModelTest`、`IndexingNoticeTest`、`RagHomeInteractionTest` | PASS：31 tests，0 failed/skipped/errors。 |
| 统一 Prompt 编辑器 | `UnifiedPromptEditorTest` + Screenshot validation + device E2E | PASS：JVM 5/5，截图基线已纳入 36/36 全量通过，AndroidTest 编译与设备主流程通过。 |
| DocEditor Material 3 第四阶段 | JVM/Lint/截图/设备/性能/独立复核 | PASS：1915 个 JVM 测试 0 failure/error、14 个既有条件 skip；56/56 截图；API 31/35/36 DocEditor 各 31/31；API 36 bulk 228 项 0 failure、3 个既有分阶段 skip，冷启动两方法各 1/1。两次性能 6/6 共 60 个样本无替换，最差 p95 35 ms、max 40 ms、PSS +12,194 KiB；终审 C0/C1/C2/C3=0、P0/P1/P2=0。 |
| Agent 首页 Material 3 连续列表 | 源码契约 + Screenshot validation + API 36 Compose | PASS：先以旧实现取得 7 项中 2 项预期 RED，再完成 9/9 契约；57/57 全量截图通过，普通英文与中文 2.0x 长标题/长描述/空描述均经人工审阅；API 36 的整行进入、菜单隔离、置顶语义、短滑不误删与空态入口共 6/6。 |
| 分层模型元数据与会话尾注 | Task 1-10 TDD/双复审 + 当前 Provider 门禁 | PASS：唯一解析入口、精确 ID、三态能力、Provider/用户覆盖迁移、引导探测、统一友好名称、AI 左侧尾注和离线目录供应链均已实现。定点模型 JVM 89/89；当前全量 JVM 1982 项，0 failure/error、14 skip；Lint 0 Error/Fatal；Screenshot 62/62；AndroidTest Kotlin 编译通过；API 31 minimum 与 API 35/36 full 设备脚本通过；当前四角色真实 Provider 流式验证通过。 |
| 空知识库、生成终态、流式淡入与设置密度 | 定向 TDD + 全量 JVM/截图/设备/双复审 | PASS：旧版孤儿索引任务只在缺失身份或文件确认不存在时清理，现代 reference 目标保留；失败/取消先发布展示终态；正常流式 chunk 即时显示并仅对尾部应用 140ms 淡入；设置与 Provider 使用连续无逐项卡片的 MD3 列表并保留 48dp 目标；数据库目录刷新不会用同 ID 迟到快照覆盖 Store 中较新的流式内容。当前全量 JVM、62 张 actual、API 31/35/36 设备矩阵通过，独立终审 Critical 0 / Important 0，剩余 3 个不阻断 Minor。 |
| 会话完整文档、导出与消息分支 | Task 1-6 TDD + 双复审 + 当前本地门禁 | PASS：输入栏 TXT/MD 以持久化快照作为完整上下文，不走 RAG；最终路由后执行 fail-closed Prompt 预算；导出可重新上传，稳定消息可创建独立分支。重试保留旧回复，失败/取消按生成前消息基线统一清理本轮全部 Assistant、工具结果与占位。定点 JVM 76 项；当前全量 JVM 2021 项，0 failure/error、14 skip；64/64 Screenshot；Lint 0 Error/Fatal；AndroidTest 编译；API 31/35/36 相关 Compose 各 8/8；Terra/Sol Critical/Important 0。本会话六项真实 Provider 变量未注入，未把历史 PASS 计为本候选复验。 |
| Material 3 总收敛施工前基线 | Task 1 当前候选 JVM/Screenshot/Lint/AndroidTest 编译 + actual 人工分类 | BASELINE：提交 `72f3f487` 上使用 `--no-build-cache --rerun-tasks` 新鲜执行：`:app:testDebugUnitTest` 为 2021 tests、0 failure/error、14 skip；Screenshot 64 张验证通过；Lint 0 Error/Fatal（401 warning、24 hint）；AndroidTest Kotlin 编译通过。人工检查 35 张设置、Provider、模型、RAG、资源与 Agent actual，并在第 6 节逐文件分类。全部仍只有深色基线，且二三级设置、模型选择器、上下文搜索、手机底部导航与平板 Rail 存在 `missing-baseline`。自动截图通过不构成新方案视觉 PASS。 |
| Material 3 总收敛 Task 6 助手会话与顶栏搜索 | TDD + JVM/Lint/Screenshot + API 31/35/36 + 双复审 | PASS：旧实现先取得 22 项中 7 项预期 RED；非空短结果搜索设备契约在 API 35 真实暴露两处位置恢复失败，改为稳定条目 ID、原始索引和像素偏移后闭合。当前全量 JVM 2046 项，0 failure/error、14 skip；Lint 0 Error/Fatal（406 warning）；79/79 Screenshot；AndroidTest Kotlin 编译；API 31/35/36 定向设备测试各 15/15。空态、双会话、长标题、2.0x 与搜索态 7 张受影响 actual 已逐张人工检查，TalkBack 置顶/删除自定义动作与返回优先级均有设备覆盖；Terra/Sol Critical 0、Important 0。 |
| Material 3 总收敛 Task 7 统一模型选择器 | TDD + JVM/Lint/Screenshot + API 31/35/36 + 双复审 | PASS：冻结 `ModelSelectionUiModel` 并通过 `ModelMetadataResolver.resolve(...)` 统一投影，通用面板与会话设置共用连续 Material 3 `ListItem`。全量 JVM 2085 项，0 failure/error、14 skip；Lint 0 Error/Fatal；81/81 Screenshot；AndroidTest Kotlin 编译；API 31/35/36 定向设备测试各 2/2。120 模型深色与长名称浅色 2.0x actual 已逐张检查，未知能力未制造支持；Terra/Sol Critical 0、Important 0。 |
| Material 3 总收敛 Task 8 设置首页与默认模型 | TDD + JVM/Lint/Screenshot + API 31/35/36 + 双复审 | PASS：设置首页收敛为六个连续分组，Provider 与四角色默认模型进入独立二级页；默认 Provider 禁止删除，额外 Provider 删除级联模型与预设；首页不做同步全量刷新，模型删除清理持久化元数据。全量 JVM 2100 项，0 failure/error、14 skip；Lint 0 Error/Fatal；85/85 Screenshot；AndroidTest Kotlin 编译；API 31/35/36 设置组合各 22/22。9 张首页/Provider/默认模型 actual 已逐张检查；Terra/Sol Critical 0、Important 0。 |
| Material 3 总收敛 Task 9 记忆、索引与检索设置 | TDD + JVM/Lint/Screenshot + API 31/35/36 + 双复审 | PASS：五个设置页迁移为无 Glass/嵌套卡片的连续 Material 3 参数表面，RAG/Search/Agent 状态所有者保持独立并使用原子 transform；全局 RAG 的状态发布与持久化由同一串行临界区保护，并有双线程字段更新契约。Tavily 保存/揭示/清除、Slider、Preset、域名、清理确认及单一 TalkBack 操作节点均有生产 Content 设备覆盖。全量 JVM 2106 项，0 failure/error、14 skip；Lint 0 Error/Fatal；90/90 Screenshot；AndroidTest Kotlin 编译；API 31/35/36 各 6/6。5 张 actual 已逐张检查；Terra/Sol Critical 0、Important 0。 |
| Material 3 总收敛 Task 10 Provider 表单与密钥交互 | TDD + JVM/Lint/Screenshot + API 31/35/36 + 双复审 | PASS：云端/本地 Provider 配置迁移为无大卡片的连续 Material 3 表单，协议使用标准单选行；测试/保存固定在响应式底部动作区，保存是唯一 filled 主动作。密钥掩码、揭示、离页清理、HTTPS 校验、本地模型恢复与测试取消合同保持不变；Testing/Success/Error 以本地化 `stateDescription` 稳定附着于测试按钮。全量 JVM 2108 项，0 failure/error、14 skip；Lint 0 Error/Fatal；90/90 Screenshot；AndroidTest Kotlin 编译；API 31/35/36 Provider 与密钥组合各 20/20。3 张 actual 已逐张检查；Terra/Sol Critical/Important/Minor 0。 |
| Material 3 总收敛 Task 11 Provider Models 摘要与独立编辑 | TDD + JVM/Lint/Screenshot + API 31/35/36 + 性能 + 三复审 | PASS：500 模型列表改为稳定 key 的摘要行，整行进入独立 `ModelEditorSheet`，Switch 不触发行点击；编辑字段、测试取消/错误、删除确认和用户覆盖来源合同保持。全量 JVM 2109 项，0 failure/error、14 skip；Lint 0 Error/Fatal；91/91 Screenshot；AndroidTest Kotlin 编译；API 31/35/36 各 16/16。固定 API 36 性能最终五轮 p95 22–23ms、最大 75ms、PSS +333KiB；静态 reference 与真实 modal 设备截图均已复审，Terra/Sol/Luna Critical/Important 0。 |
| Material 3 总收敛 Task 12 外观与其余设置表面 | TDD + JVM/Lint/Screenshot + API 31/35/36 + 设备 actual + 双复审 | PASS：外观页使用应用级 Store 与 `ThemeViewModel`，系统/浅色/深色和动态色立即持久化；Backup、Skills、Token Usage、Local Models、Developer 迁移为连续 Material 设置表面，业务动作集合保持。全量 JVM 2115 项，0 failure/error、14 skip；Lint 0 Error/Fatal；95/95 Screenshot；AndroidTest Kotlin 编译；API 31/35/36 主题/备份各 6/6。API 36 六页各生成英文深色、中文浅色、英文浅色 2.0x、中文深色 2.0x 共 24 张 actual，主控发现并关闭技能 ID、本地插槽和设备信息三处响应式问题；Terra/Sol Critical/Important 清零。 |
| Material 3 总收敛 Task 13 自适应语义色与富文本主题 | 全树静态合同 + JVM/Lint/Screenshot + API 36 WebView + 双复审 | PASS：冻结 69 路径完成全树迁移，正式 UI 不再消费静态深色、Glass、`NexaraColors` 或 `NexaraGlassCard`；原生页面使用 `MaterialTheme.colorScheme`，领域状态与代码色由主题内 `NexaraDomainColors` 提供。Markdown/WebView/图表 CSS 按当前主题重建；API 36 真 WebView 在同一 Activity 内由深色 `rgb(19,19,21)` 切换为浅色 `rgb(251,248,251)`，1/1 通过。全量 JVM 2117 项，0 failure/error、14 skip；Lint 0 Error/Fatal（420 warning）；96/96 Screenshot；AndroidTest Kotlin 编译。24 张受影响 reference 与新增浅色富 Markdown actual 已逐张检查；Terra Critical/Important/Minor 0，Luna P0/P1 0。 |
| Material 3 总收敛 Task 14 本地与分支 CI 门禁 | JVM/Lint/Screenshot + API 31/35/36 + 性能 + actual + 双复审 | PASS：当前本地完整 JVM 为 2119 项、0 failure/error、14 skip；Lint 0 Error/Fatal、420 warning、25 hint；Screenshot 96/96。API 31/35/36 冻结类各 101/101，broader harness 分别 39/41/41 个显式测试并各有 1 个设计内 skip，三档 exit 0。治理 HEAD `3535735f` 的分支 CI 以 2119/0/0/14、96/96、0 Error/Fatal 和 API 31/35/36 `exit-code=0` 首轮通过。500 模型有效 run 1/run 4 各保留 45 条 raw histogram 并独立复算通过，较差样本 p95 35ms、max 51ms、PSS +2763KiB；系统污染样本保留为 `FIXTURE_INVALID`。四角色真实 Provider、当前签名 R8 APK 静态验真及同源历史候选 API 35/36 同哈希冷安装均通过；用户真机人工项按 beta 风险接受处理。 |
| Release 日志源码契约 | `ReleaseLoggingSourceContractTest`、`PostProcessorTest` + minified/签名 APK 日志 | PASS：源码契约 3/3 + 3/3；API 35/36 minifiedTest 冷启动与文档分享导入无 crash/ANR。2026-07-22 同源稳定证书候选在 API 35/36 冷启动和语言选择后均无应用 crash/ANR，报告文本的 Bearer、密钥、CGNAT 与签名材料模式扫描无命中；当前 `d226395f...` 只完成静态验真，最终 workflow 会重新执行日志门禁。 |
| APK 验证器 | `python3 -m unittest scripts/tests/test_verify_release_apk.py` + 当前签名候选验真 | PASS：验证器既有 23 tests 保持通过；当前 18,367,648-byte 稳定证书签名 APK 的包身份、唯一签名者、登记证书、敏感内容、GGUF/本地推理制品、ZIP/体积与 SHA-256 全扫描通过，SHA-256 为 `d226395f2da82c943698e5e78a0ee363ddef5839389543382ce55d90ad732146`。 |
| Metro 开发者 TUI | `node --test scripts/tests/nexara-metro-tui.test.js` | PASS：49 tests，0 fail；真实 adb 长时间运行不属于发行 APK 门禁。 |
| Release workflow 可靠性契约 | Python/Shell 契约、Ruby YAML parse、独立复审 | PASS：`workflow_dispatch` 候选模式 fail-closed 要求固定分支、检出 HEAD、`GITHUB_SHA` 与管理员控制的完整 `NEXARA_RELEASE_COMMIT_SHA` 四者一致；候选继续执行设备、R8 签名和 API 35/36 冷安装链，只把 publish 精确限制为 `refs/tags/v0.2-beta`。当前 28/28 workflow 契约与 23/23 APK verifier 契约、YAML 解析通过；发布改为 draft-first，只有 APK/checksum 全量回读同哈希后才公开 prerelease，重跑同步更新标题与发行正文。因 Compose Screenshot 插件在与 `clean` 或其它 Android 变体同一 Gradle 调用时会触发 `androidx.fragment.R` InnerClasses 初始化冲突，签名构建按独立 `clean`、独立 Screenshot、独立 JVM/Lint/Debug/Release 三个 fail-closed 进程执行。 |
| Room 关闭重开与分享索引重试 | `VectorizationQueueRoomTest`、`ShareImportMimeTypeTest`、API 36 Compose、Debug 签名非调试 R8 `minifiedTest` 黑盒 | PASS：文件型 Room close/reopen 后恢复失败任务并复用同一 task id 重置为 pending；已知 MIME 紧凑显示单测通过；API 36 Compose 2/2，含 2.0x 字体四动作显示、触控高度与边界断言；混淆包 TXT 首次失败与再次重试均可观察且无 crash/ANR。该测试构建与生产 release 使用同类 R8/资源收缩边界，但包名和签名不同，不冒充最终签名 APK。 |
| Android 截图证据链 | `test-normalize-android-screencap-png.py`、minified 黑盒 8 张 `screen-*.png`、真实异常 PNG 像素比对 | PASS：标准库工具覆盖 PNG CRC、filter 0–4、坏输入拒绝、同路径原子替换；历史异常图归一化前后 1080×2400 RGB 逐像素一致且 alpha=255。最终 API 36 黑盒 8/8 PNG 均为 alpha=255、无 raw/current/previous/tmp 残留；DOCX indexed 与 TXT refailed 经无 alpha JPEG 视觉副本复核，画面、状态和动作完整。 |
| 真实 LLM 四角色兼容 | `:app:realLlmIntegrationTest` + Generic OpenAI-compatible 定向 JVM 测试 | PASS：同一 Router 中四个模型各请求一次；高速文本、高智力文本、多模态 MiniMax 与均衡多模态均取得有效流式 payload 和完成终态，图片输入通过，并兼容 `delta.reasoning`。取消传播由受控在途协议 Flow 和保持打开的 MockEngine SSE 响应独立验证，避免用可能已自然结束的真实高速响应作假阳性证明。真实任务禁用 up-to-date/构建缓存后新鲜执行成功（45s），凭据未落盘。 |
| 真机首轮缺陷回归 | `OnboardingAndroidEndToEndTest`、`AccessibilitySmokeTest`、`ProviderModelsAccessibilityTest` + API 36 手工入口验证 | PASS：21 项设备回归 0 failed，1 个仅供分阶段 force-stop 的用例按设计 skip；首聊成功后保持聊天页并可返回主界面，知识库/记忆设置/检索设置依次真实点击显示，聊天胶囊和模型管理 48dp 触控层及紧凑可见面均通过。 |
| 首次远端 Android CI | [run 29389720531](https://github.com/Promenar/Nexara/actions/runs/29389720531) | FAIL（已定位并在后续候选闭环）：JVM/Lint/构建成功，API 31 成功；API 35 因权限弹窗只搜索活动根而失败，API 36 因 onboarding 过早访问 ProviderManager 而失败。 |
| 第二次远端 Android CI | [run 29398804571](https://github.com/Promenar/Nexara/actions/runs/29398804571) | FAIL 后被后续 push 取消重跑：quality 与 API 31 成功；API 35 因测试真实旋转宿主 Activity 后 Compose root 被销毁而失败，API 36 因托管模拟器 System UI 无响应导致通知权限按钮不可达而失败。本机 API 36 完整设备链同提交通过；API 35 已改用 Compose 窗口/字体配置覆盖并连续 4 轮通过。 |
| 前一候选远端 Android CI | [run 29401903929](https://github.com/Promenar/Nexara/actions/runs/29401903929) | PASS：目标提交 `10d6761`；quality 18m09s、API 31 9m28s、API 35 11m03s、API 36 12m30s，四项全部成功。 |
| 历史应用候选远端 Android CI | [run 29408429117](https://github.com/Promenar/Nexara/actions/runs/29408429117) | PASS：目标提交 `bf6f87c`；quality 18m39s、API 31 9m53s、API 35 10m56s 成功。API 36 首轮被托管模拟器 `System UI isn't responding` 系统弹窗阻挡权限弹窗；保留截图/logcat 后只重跑失败作业，第二轮同一断言 13m03s 成功，最终 run conclusion 为 `success`。 |
| Task 14 首轮远端 Android CI | [run 29828290368](https://github.com/Promenar/Nexara/actions/runs/29828290368) | FAIL：目标提交 `11bb77ad`；模型目录校验通过，quality 在 96 张 Screenshot 中准确暴露 9 张含时间文本的 reference 漂移后退出 1，设备矩阵按依赖未启动。下载并检查远端 rendered/diff 后确认 Ubuntu UTC 与东八区 reference 只在时间文本上相差 8 小时；主控在本机外部 `TZ=UTC` 下复现定点 RED。修复将 Screenshot validation/update 两类 JVM 任务统一固定为 `Asia/Shanghai`，不改产品代码或 reference；同条件定点和完整 96/96 已 GREEN，等待新提交远端复验。 |
| Task 14 第二轮远端 Android CI | [run 29830080792](https://github.com/Promenar/Nexara/actions/runs/29830080792) | FAIL：目标提交 `622d8908`；首轮 9 张时区差异全部消失，quality 仅剩 3 张 streaming preview 失败，设备矩阵按依赖未启动。下载并逐图检查远端 reference/rendered/diff 后确认布局、时间与文本完全一致，差异只覆盖流式正文的透明度，根因为 140ms 尾部淡入在不同机器捕获到不同中间帧。`LocalInspectionMode` 仅在 Preview/Screenshot 检查模式固定动画终态，真实应用继续执行淡入；新增纯函数分支测试，两轮 `TZ=UTC` 定点 rendered SHA-256 逐张一致，完整 Screenshot 96/96。3 张 reference 只在确认差异范围后更新，等待第三轮远端复验。 |
| Task 14 文档闭合提交远端 Android CI | [run 29835588741](https://github.com/Promenar/Nexara/actions/runs/29835588741) | PASS：目标提交 `29c1bbad`；quality 17m13s、API 31 10m36s、API 35 12m05s、API 36 13m14s，四项均在首轮成功。该结果验证应用候选与正式文档闭合提交，不替代签名 release workflow。 |
| 当前分支 CI 启用后的两轮竞态暴露 | [run 29839248199](https://github.com/Promenar/Nexara/actions/runs/29839248199)、[run 29840437952](https://github.com/Promenar/Nexara/actions/runs/29840437952) | FAIL（已修复）：两轮均由 `VectorizationQueueRoomTest` 在 worker 与 Room 快照断言之间的调度竞态导致 2118 项中 1 项失败，设备矩阵按依赖跳过。第一次修复只覆盖冷启动补建场景，第二轮继续暴露失败任务 Update 场景，因而没有把单次绿色当作闭合证据。最终将严格 `pending` 快照与 worker 调度器隔离，其余持久化断言使用阻塞索引服务保留可观察中间态；生产代码未改变。 |
| 历史应用候选远端 Android CI | [run 29851653351](https://github.com/Promenar/Nexara/actions/runs/29851653351) | PASS：目标提交 `702181e5`；quality 19m54s、API 31 9m26s、API 35 12m02s。API 36 首轮被托管模拟器 `System UI isn't responding` 对话框遮挡通知权限按钮，Nexara crash 记录为空；保留失败产物后仅重跑失败 job，第二轮 10m42s 完整通过。远端 JVM 2118 项、0 failure/error、14 skip；Lint 0 Error/Fatal；Screenshot 96/96；AndroidTest 编译成功。重试产物确认 API 36 full 41/41、设计内 expected-death checkpoint 1 项、`exit-code=0`，`crash-log.txt` 与 `crash-after-tests.txt` 为空。 |
| 设备 harness 补强首轮远端 Android CI | [run 29862967856](https://github.com/Promenar/Nexara/actions/runs/29862967856) | FAIL（已定位并完成本地修复，待新提交复验）：目标提交 `4a08a3c0`；模型目录校验通过，quality 的 2118 项 JVM 中仅 `VectorizationQueueRoomTest.配置reset等待旧queue停止并把处理中任务交接给新queue恢复` 失败，设备矩阵按依赖未启动。下载的 XML 证明断言落在 completed 持久任务已删除、内存队列 finally 尚未移除同一任务的合法瞬间；测试现先运行同一 `TestCoroutineScheduler` 到稳定态，再同时断言 Room 与队列为空，生产代码未改变。定点、完整测试类 28 项及全量 JVM 2118 项均以 `--no-build-cache --rerun-tasks` 新鲜通过，远端复验仍为 PENDING。 |
| 设备 harness 补强远端复验 | [run 29864872637](https://github.com/Promenar/Nexara/actions/runs/29864872637) | PASS：目标提交 `52d038a6`；quality 21m00s、API 31 10m09s、API 35 12m48s、API 36 10m23s，四项均成功。下载产物回读确认 JVM 2118 项、0 failure/error、14 skip；Lint 0 Error/Fatal（420 warning、25 hint）；Screenshot 96/96。API 31/35/36 均 `exit-code=0`，测试前后 crash 日志为空；API 35/36 的锁屏生命周期、无 Key restore relay 与 no-replay 均取得精确单测终态，expected-death 记录为目标进程退出且 relay 使用独立 PID、`payload=none`。首轮竞态失败和独立修复提交均保留，没有用盲目重跑替代根因闭合。 |
| 设备 harness 文档后继远端稳定性复验 | [run 29867832809](https://github.com/Promenar/Nexara/actions/runs/29867832809) | FAIL（已定位并完成确定性本地修复，待新提交复验）：目标提交 `af58c460`；quality 的 2118 项 JVM 中 `VectorizationQueueRoomTest` 出现 2 项失败，设备矩阵按依赖跳过。XML 证明其一仍把 `runCurrent()` 误当作 Room DAO 完成屏障，其二的可取消测试门闩被生产 supersede 取消合法穿透；现以 finally 到达/放行信号精确断言 `interrupted -> Room 删除 -> 内存移除` 三阶段，并仅在测试钩子内使用 `NonCancellable`，所有门闩均在 `finally` 放行。生产代码未改变；精确用例、完整测试类多轮和全量 JVM 2118 项均新鲜通过，远端复验仍为 PENDING。 |
| 一次性通知停止修复前远端矩阵 | [run 29870166426](https://github.com/Promenar/Nexara/actions/runs/29870166426) | FAIL（产品边界已修复，宿主异常已隔离）：目标提交 `6fd0227b`。attempt 1 的 quality、API 31 成功；API 35 真实暴露已消费的旧 stop `PendingIntent` 可再次启动 Service 并短暂恢复通知，API 36 在权限弹窗前被 `System UI isn't responding` 阻断。只重跑失败 job 后 API 36 完整通过且 crash buffer 为空；API 35 attempt 2 在更早的通知权限阶段被 `Quickstep isn't responding` 阻断，未到达后台生命周期，不能误记为第二次产品失败。主控先在 API 35 证伪动态 Receiver 方案，再按 Android 官方防重放语义保留显式私有 Service 并增加 `FLAG_ONE_SHOT`；当前未提交候选本机 API 35 full 全部通过，等待新提交远端复验。 |
| 一次性通知停止当前候选远端矩阵 | [run 29877797382](https://github.com/Promenar/Nexara/actions/runs/29877797382) | PASS：目标提交 `98ec89bf`；quality 19m18s、API 31 10m16s、API 35 12m38s、API 36 12m28s，四项均首轮成功。六个 artifact ZIP 的下载 SHA-256 与 GitHub API digest 全部一致；XML 回读为 JVM 2119 项、0 failure/error、14 skip，Screenshot 96/96、0 failure/error/skip，Lint 0 Error/Fatal（420 warning、25 hint）。API 31 minimum、API 35/36 full 均 `exit-code=0`，三档测试前后应用 crash buffer 为空；API 35/36 的真实锁屏/唤醒/旋转、首次 stop、旧 action 二次发送拒绝及通知权限拒绝/允许均取得独立测试终态。每档 onboarding 保留 1 个设计内 `forceStopPhaseCheckpoint` skip，不记作 PASS。 |
| 当前治理 HEAD 远端矩阵 | [run 29886387249](https://github.com/Promenar/Nexara/actions/runs/29886387249) | PASS（attempt 2）：目标提交 `e7e7bd4f`，相对生产提交只变更治理文档。quality 21m07s、API 31 10m08s、API 35 13m00s 首轮成功；API 36 首轮在 `MainActivityNotificationE2eTest` 点击系统拒绝按钮前被 `System UI isn't responding` ANR 对话框遮挡，失败截图与 UI tree 明确属于 `com.android.systemui`，Nexara crash 记录为空。保留首轮 artifact 后仅重跑失败 job，API 36 第二轮 13m47s 完整成功。API 31 为 21 PASS + 1 DESIGN-SKIP，API 35/36 各为 44 PASS + 1 DESIGN-SKIP；三档 `exit-code=0` 且两份 crash buffer 均为 0 bytes。第二轮 API 36 artifact ZIP SHA-256 `48064fbddf4866a1f951b09685696b0caeb9cfb30f7a28ca86720bf7289855b4` 与 GitHub digest 一致，通知权限拒绝/允许各 1/1。 |
| 最新远端分支 HEAD 矩阵 | [run 29889114257](https://github.com/Promenar/Nexara/actions/runs/29889114257) | PASS（首轮）：目标提交 `5e8d468d`，相对生产提交只更新正式治理文档。quality 20m51s、API 31 10m03s、API 35 13m00s、API 36 13m22s，四个 job 均为 `success`。该结果确认当前远端分支 HEAD 可复现完整矩阵；不为纯文档 CI 结果继续制造追认提交。 |

| 宿主 ANR 恢复与证据链本地候选 | 本地可见 API 35/36 完整设备矩阵 | PASS：通知权限测试仅允许 `Quickstep isn't responding`、`Pixel Launcher isn't responding`、`System UI isn't responding` 三个完整标题，单次权限操作最多恢复一次；必须先截图、再确认可用 Wait 且点击成功，最后要求权限窗口在 3 秒内恢复，无通用 ANR 匹配、无多次重试、无 skip。API 35 实际命中 Pixel Launcher ANR，恢复前截图已在下一次 `pm clear` 前回拉宿主；API 36 首轮暴露前台服务启动通知尚未替换为完整通知时的测试竞态，现等待唯一通知真实包含停止动作，缺失时给出语义化失败而非 NPE。脚本对测试成败都先回拉全部已发现截图，每次 `adb pull` 显式检查退出状态且只统计成功回拉，之后才校验预期对话框是否齐全；EXIT trap 对未完成的回拉再做一次最佳努力。最终当前脚本与测试 APK 在 API 35/36 各产生 26 个含 `OK` 的 instrumentation 结果文件，两档 `exit-code=0`，测试前后 crash buffer 均为 0 bytes，各保留 4 张权限流程截图；onboarding 中设计内 checkpoint skip 仍独立记账。当前候选证据位于 `native-ui/app/build/reports/host-system-anr-hardening-api35-final4-20260722/` 与 `host-system-anr-hardening-api36-final4-20260722/`；真实 Pixel Launcher ANR 恢复前截图保留于 API 35 `final2` 证据目录。 |

## 3. 最终集成质量门禁

| 项目 | 状态 | 证据 |
|---|---|---|
| 全量 JVM | PASS | 当前工作树从 `clean` 状态完整执行 `:app:testDebugUnitTest`：2119 tests、0 failure/error、14 个既有条件 skip；新增 1 项契约测试把 `FLAG_ONE_SHOT` 精确绑定到通知 stop action 构造块。run `29877797382` 下载 XML 独立汇总得到同样的 2119/0/0/14。 |
| Android Lint | PASS | Task 14 当前工作树与 run `29877797382` 均为 0 Error/Fatal、420 warning、25 hint；远端 XML 已回读。 |
| Screenshot validation | PASS | Task 14 当前工作树与 run `29877797382` 均为 96/96、0 failure/error/skip；96 张 actual 已在本地阶段全部人工检查。文档标题 2.0x 与流式终态 reference 已完成确定性修复；静态截图不冒充动效连续视频。 |
| Android instrumentation | PASS + 1 DESIGN-SKIP / API | 当前一次性 stop 候选在本地可见 API 35 完整设备脚本、run `29877797382` 的 API 31 minimum/API 35/36 full、治理 HEAD run `29886387249`、run `29889114257` 及当前 HEAD run `29893140602` 中均通过。通知权限拒绝/允许、真实锁屏/唤醒/旋转、通知返回准确会话、首次 stop、第二次发送明确 `CanceledException`、静默窗口及冷启动 stop/track 均通过；已回读矩阵的三档应用 crash buffer 为空。最新完整 artifact 回读中的 API 36 为 44 项真实通过、1 项设计内 skip。每档 onboarding 的 `forceStopPhaseCheckpoint` 只允许主控通过 `onboardingPhase` 显式调用，常规套件各跳过 1 次；该 skip 单独记账，不计入 PASS 数。 |
| Provider Models 500 模型性能 | PASS | API 36 Pixel_7 以 `-no-window -no-snapshot -gpu host` 独占运行、三项动画缩放为 0。有效 run 1/run 4 均为 45 条 raw、单一窗口标记、污染 0，独立复算通过；较差 p95 35ms、max 51ms、PSS +2763KiB，低于 50ms/150ms/64MiB 门槛。run 2/run 3 各命中一条约 180 秒周期的系统包事件，分类为 `FIXTURE_INVALID` 并保留。证据：`native-ui/app/build/reports/provider-models-performance-task14-final-20260721-191942/`。 |
| Debug APK | PASS | 当前工作树 `--rerun-tasks :app:assembleDebug` 新鲜执行通过；最终 tag workflow 仍须重新 clean 构建发行包。 |
| Release APK | PASS（本地签名候选） | 当前稳定证书签名 R8 APK 为 18,367,648 bytes、SHA-256 `d226395f2da82c943698e5e78a0ee363ddef5839389543382ce55d90ad732146`；已完成独立 clean 构建、包身份、唯一签名者、登记证书、R8、16 KiB zipalign、checksum 与敏感/GGUF 扫描。同源稳定证书候选的 API 35/36 冷安装、冷启动和 crash/ANR 已通过；tag workflow 将对最终重建产物重新执行并以远端 checksum 为发布事实。 |

建议最终命令（以实际 Gradle task 为准）：

```bash
cd native-ui
./gradlew --no-daemon --stacktrace --no-build-cache clean
./gradlew --no-daemon --stacktrace --no-build-cache \
  :app:validateDebugScreenshotTest
./gradlew --no-daemon --stacktrace --no-build-cache \
  :app:testDebugUnitTest \
  :app:lintDebug \
  :app:assembleDebug \
  :app:assembleRelease
```

## 4. 签名、R8 与 APK 安全

| 项目 | 状态 | 待回填证据 |
|---|---|---|
| 稳定发行证书 | PASS | 本地发行证书有效期 2025-12-29 至 2053-05-16；SHA-256 `00:BE:4C:DD:83:78:AA:FB:D7:0E:BC:43:E9:71:52:37:91:CC:07:DE:EA:D6:31:E0:6D:CF:15:5D:EE:EE:38:02`，与 GitHub `release` environment 登记值逐字节一致。未记录别名或密码。 |
| R8 mapping | PASS | 当前签名 release mapping 为 100,602,563 bytes，非空。 |
| R8 seeds/usage/configuration | PASS | 当前签名 release seeds 773,487 bytes、usage 11,272,586 bytes、configuration 67,338 bytes，均非空。 |
| APK 包身份 | PASS | 当前统一验证器确认 `com.promenar.nexara.native`、versionCode 2、versionName `0.2-beta`。 |
| 单一签名者/证书匹配 | PASS | 当前 `apksigner` 确认单一 signer，证书与 GitHub `release` environment 登记 SHA-256 一致。 |
| ZIP/体积/敏感内容扫描 | PASS | 当前 ZIP 完整、18,367,648 bytes，未命中凭据、私钥、本机路径、私网测试地址或其它受控敏感模式。 |
| zipalign | PASS | 当前 APK 通过 `zipalign -c -P 16 4`。 |
| SHA-256 | PASS | 当前本地候选值 `d226395f2da82c943698e5e78a0ee363ddef5839389543382ce55d90ad732146`，checksum 文件非空；远端 workflow 重建产物以 Release checksum 回读为准。 |
| Release logcat | PASS（同源历史候选） | 2026-07-22 的 `2627b007...b674d` APK 在 API 35/36 冷启动及语言选择后进程存活、前台 resumed，crash buffer 无目标包 crash/ANR，报告敏感模式扫描无命中；当前 `d226395f...` 只完成静态验真，最终 workflow 会重新执行。 |
| GGUF/llama/ggml 排除 | PASS | 当前统一验证器确认签名 APK 未携带 GGUF 或本地推理原生库。 |

## 5. 冷安装与 minified 业务验真

| 场景 | API 35 | API 36 | 证据要求 |
|---|---|---|---|
| 卸载旧包、侧载签名 APK、冷启动、无 crash/ANR | PASS | PASS | 两档均安装 2026-07-22 同源稳定证书候选 SHA-256 `2627b007...b674d`；设备回拉 `base.apk` 与当次本地产物逐字节一致，且各自 `apk-digests.txt` 记录完整本地/设备 SHA-256 与 `byte_identical=true`。冷启动、前台 resumed、5 秒存活和 crash/ANR 检查通过；最终 tag workflow 必须对远端重建 APK 重新执行。 |
| Room 新建、强停、重启读取 | DEVICE E2E PASS / SIGNED PENDING | DEVICE E2E PASS / SIGNED PENDING | `mainactivity-new-session` 与 onboarding 分阶段强停链通过；非调试签名包实际数据仍待物理真机验收。 |
| 中英文 onboarding 至首聊 | DEVICE E2E PASS / SIGNED PENDING | DEVICE E2E PASS / SIGNED PENDING | 可恢复语言、Provider、模型与首聊 checkpoint 通过；签名包人工首聊仍待真机。 |
| Provider 表单与模型选择 | DEVICE E2E PASS / SIGNED PENDING | DEVICE E2E PASS / SIGNED PENDING | 无真实 Key 的 Provider、模型选择、持久化与 2.0x 设备合同通过；签名包黑盒仍 PENDING。 |
| 无 Key 备份 round-trip | DEVICE E2E PASS / SIGNED PENDING | DEVICE E2E PASS / SIGNED PENDING | 未加密包确认 `containsSecrets=false`，导出、stage、relay 杀进程恢复、数据/偏好回读、可备份 Key 清除、排除项保留和 no-replay 全链通过。 |
| 含 Key 加密备份 round-trip | DEVICE E2E PASS / SIGNED PENDING | DEVICE E2E PASS / SIGNED PENDING | 使用虚构测试凭据完成加密导出、relay 杀进程恢复、Key 回读与 no-replay；未在日志或截图暴露内容。 |
| DOCX/PDF 解析 | PASS | PASS | API 35/36 `minifiedTest` APK 使用真实 PDF/DOCX fixture 完成分享导入，覆盖 R8/反射边界；证据位于对应 `artifacts/android-minified-blackbox-*` 目录。 |
| RAG 导入/索引/重试 | PASS | PASS | API 35/36 的 Debug 签名、非调试 R8 `minifiedTest` 均使用真实 TXT fixture 完成导入，观察到索引失败、启用 Retry、点击后再次失败并可再次重试；API 35 证据目录为 `artifacts/android-minified-blackbox-api-35-arm64-rag-room-20260715-final/`，exit=0、crash buffer 为空。该构建不替代当前签名 APK 物理真机黑盒。 |
| 后台/锁屏/旋转/停止/返回 | DEVICE E2E PASS / SIGNED PENDING | DEVICE E2E PASS / SIGNED PENDING | 真实系统休眠/唤醒、横屏、单一持续通知、通知返回准确会话、首次与重复停止不复活通过；终态唯一另由 `DefaultGenerationCoordinator` 确定性状态机测试覆盖。签名包物理真机仍 PENDING。 |

当前 workflow 的签名 APK smoke 只覆盖校验、冷安装、启动、前台存活与 crash/ANR；上表 `DEVICE E2E PASS` 是可测试构建的直接业务证据，不得冒充非调试签名 APK 黑盒或物理真机验收。受 onboarding 成功 Provider 门禁与 release HTTPS 限制，未引入任何发行后门来强行自动化这些签名包场景。

## 6. UI、视觉与无障碍矩阵

所有截图必须来自最终集成后的当前构建，逐张人工审阅。旧安装包或历史 `/tmp` 截图只能作线索，不能作为本版 PASS 证据。

截至 2026-07-22，当前 96 张 Screenshot validation 全部通过，主控已检查本轮新增及受影响 actual，覆盖完整文档中英文、大字体、长文件名、聊天 ready/reasoning/stream/error、助手/会话空态与搜索态、120/500 模型目录、长模型名、未知能力、独立模型编辑器，以及设置首页、Provider、四角色默认模型、五个检索设置页、Provider 表单、外观/备份、深浅富 Markdown、知识库空态和流体手机导航等基线。API 31/35/36 的 Task 6 助手/会话各 15/15、Task 7 模型选择器各 2/2、Task 8 设置组合各 22/22、Task 9 检索设置各 6/6、Task 10 Provider 与密钥组合各 20/20、Task 11 Provider 模型摘要/编辑各 16/16、Task 12 主题/备份各 6/6；API 36 另检查 Task 12 六页四种模式共 24 张设备 actual，并以真 WebView 验证 Task 13 同 Activity 深浅主题切换 1/1，API 35 流体导航 6/6。2026-07-22 同源稳定证书候选 `2627b007...b674d` 已在 API 35/36 完成同哈希冷安装、冷启动、进程存活和 crash buffer 复验；当前 `d226395f...` 未执行冷安装，最终 workflow 必须重新闭合。流式输出与导航动效的连续性由时序契约和设备状态覆盖，静态截图不冒充连续视频。人工听觉与全焦点遍历属于已接受的 beta 后续验证项。

### Material 3 总收敛 Task 1 actual 分类

以下标识对应 `native-ui/app/build/outputs/screenshotTest-results/preview/debug/rendered/` 内文件名的测试函数前缀。分类只冻结迁移目标，不表示新视觉已经通过。

- `keep`（6）：`agentHubEmptyLargeFontReleasePreview`、`agentHubPopulatedLargeFontReleasePreview`、`agentHubReleasePreview`、`ragDetailsKgLongLandscapeReleasePreview`、`ragDetailsRetrievedPhoneReleasePreview`、`ragDetailsWebLargeFontReleasePreview`。
- `migrate-form`（4）：`providerFormAddReleasePreview`、`providerFormEditActionsLargeFontReleasePreview`、`providerFormLocalFailureReleasePreview`、`providerMaskedKeyReleasePreview`。
- `migrate-list`（9，设置/模型）：`onboardingModelListReleasePreview`、`providerModelsChineseLandscapeReleasePreview`、`providerModelsReleasePreviewChineseLargeFont`、`providerModelsReleasePreviewEnglish`、`userSettingsAppChineseCompactLargeFontReleasePreview`、`userSettingsAppChineseTabletReleasePreview`、`userSettingsProviderEmptyChineseLandscapeLargeFontReleasePreview`、`userSettingsProviderEmptyChineseLargeFontReleasePreview`、`userSettingsProviderLargeFontReleasePreview`。
- `migrate-list`（12，RAG）：`ragFilesPanelDeepTreeLargeFontReleasePreview`、`ragFolderContentLandscapeReleasePreview`、`ragFolderContentPhoneReleasePreview`、`ragFolderSelectedLargeFontReleasePreview`、`ragHomeDocumentsLandscapeReleasePreview`、`ragHomeDocumentsReleasePreviewEnglish`、`ragHomeMemoryChineseLargeFontReleasePreview`、`ragHomeMemoryContentLargeFontReleasePreview`、`ragHomeMemoryContentPhoneReleasePreview`、`ragHomeMemoryDeleteConfirmationReleasePreview`、`ragHomeMemoryExpandedPhoneReleasePreview`、`ragHomeSelectedDocumentsPhoneReleasePreview`。
- `migrate-list`（4，资源管理）：`resourceExplorerFilesImportEnglishReleasePreview`、`resourceExplorerRecycleDeleteConfirmChineseLargeFontReleasePreview`、`resourceExplorerRecycleFailureLandscapeReleasePreview`、`resourceExplorerRecycleRunningChineseLargeFontReleasePreview`。
- `theme-blocked`：上述 35 张仅覆盖现有深色方案；浅色、系统跟随和动态色均无 current actual，不得用这些图片批准主题迁移。
- `missing-baseline`：二三级设置首页、记忆设置、检索设置、主题选择、通用模型选择器、会话上下文搜索、手机底部导航和大屏 `NavigationRail` 尚无本轮专用基线，按后续 Task 补齐。

### Material 3 总收敛 Task 2 主题基础验证

- RED：定点测试在 `compileDebugUnitTestKotlin` 因缺少冻结的主题枚举、偏好模型、Store、浅色 ColorScheme、Application 所有者与 Settings 委托接口而失败；未使用 `@Disabled`、注释或临时 stub。
- GREEN：`ThemePreferenceStoreTest`、`NexaraThemeTokenTest`、`SettingsViewModelTest`、`BackupPreferenceInventoryTest` 共 28 项，0 failure/error/skip；Debug Kotlin 与 AndroidTest Kotlin 编译通过。
- 回归：全量 JVM 2028 项，0 failure/error、14 skip；Screenshot 64/64；Lint 0 Error/Fatal；`git diff --check` 通过。
- 复审：独立 Sol 复审 Critical 0、Important 0；保留 1 个非阻断 Minor，即系统栏与主题分支尚无设备级直接验证，将在后续主题 UI/设备任务闭合。
- 阶段边界：当前只验证主题状态、持久化、备份白名单和双 ColorScheme。浅色、系统跟随、动态色的 current actual 仍为 PENDING，不得据此批准全站主题迁移。

### Material 3 总收敛 Task 3 共享原语验证

- RED：定点 JVM 9 项中 6 项只因 `NexaraSettingsSection`、`NexaraSearchTopBar` 缺失而失败；AndroidTest 编译只因新 Top Bar 尚不存在而失败，未使用 disabled、注释或 stub。
- GREEN：定点 JVM 9/9；全量 JVM 2034 项，0 failure/error、14 skip；AndroidTest Kotlin 编译通过。
- 设备：可见 Pixel 7 API 36 上新增两个方法 2/2，通过 2.0x 长标题、48dp、常态 actions、自动焦点、文本输入、IME Search、清除、两级系统 Back、0x reduced-motion 两帧完成以及分隔线与未合并正文 bounds 对齐。
- 视觉：Screenshot 64/64；唯一变化为 `backupSettingsReleasePreview` 的分组标题从旧 10sp 全大写静态样式迁移到 Material `labelLarge`，主控逐张对照无裁切后更新 reference。Lint 0 Error/Fatal、403 warnings、24 hints。
- 复审：独立 Terra 首轮提出 1 个 Important 测试证据缺口；补齐真实设备交互与 bounds 后复审 Critical/Important/Minor 均为 0，Verdict Approve。
- 阶段边界：共享原语已可供后续页面消费，但大部分设置/搜索页面尚未迁移，不能把本任务描述为全站视觉完成。

### Material 3 总收敛 Task 4 自适应导航验证

- RED：AndroidTest 与 Screenshot 测试源先编译通过；API 36 导航 4 项中新增两项因缺少三个标准 Tab 标签失败，旧宽度断点/960dp 约束继续通过；四张新预览因 reference 缺失使 Screenshot 68 项中 4 项失败。
- GREEN：手机使用单层 tonal `Surface + NavigationBar + NavigationBarItem`，大屏保留 `NavigationRailItem`；移除旧自绘 Row、glow、`drawBehind`、`setShadowLayer` 与 scale-only feedback。
- 设备：可见 Pixel 7 API 36 `AdaptiveNavigationTest` 4/4；覆盖 599/600dp 断点、三项 Role.Tab/selected/48dp、点击切换、Rail 唯一性，并真实滚动 50 项列表到末项验证其底边不越过导航坞顶边。
- 视觉：Screenshot 68/68；手机/平板、深色/浅色四张 actual 逐张检查，手机保持克制外边距和 Material 选中指示，平板 Rail 稳定，无裁切、标签溢出或低对比。
- 回归：全量 JVM 2034 项，0 failure/error、14 skip；Lint 0 Error/Fatal、403 warnings、24 hints；AndroidTest/ScreenshotTest Kotlin 编译与 `git diff --check` 通过。
- 复审：独立 Luna Critical/Important 为 0，Verdict Approve；其滚动末项 Minor 已补真实设备断言关闭，占位内容截图仅承担隔离导航视觉的职责。

### Material 3 总收敛 Task 4.1 流体胶囊导航返修验证

- RED：`FluidNavigationMotionTest` 首次进入 `compileDebugUnitTestKotlin`，只因缺少 `fluidNavigationIndicatorScale` 与 `fluidNavigationTargetIndex` 失败；未提前实现生产接口，未使用 disabled、注释或临时 stub。
- GREEN：手机端改为 64dp 全胶囊 tonal Surface 与单一 48dp 移动指示器，选中项展示图标和标签、未选中项展示图标；弹簧位移叠加横向拉伸/纵向收缩，快速反向切换从当前形变接续，0x 动画缩放直接到终态。每个 Tab 的原生 bounded ripple 由 `CircleShape` 裁剪，不再暴露矩形槽位；600dp 及以上 Rail 保持不变。
- 设备：可见 Pixel 7 API 35 `AdaptiveNavigationTest` 6/6；覆盖 599/600dp 断点、三项 Role.Tab/selected/48dp、内容避让、快速连点、形变中点、反向切换首帧连续性及 `MotionDurationScale=0`。
- 视觉：Screenshot 75/75；深色、浅色、英文 2.0x、紧凑横屏四张手机 actual 逐张检查，导航坞高度、选中标签、长字体、系统 Insets 均无裁切或重叠。API 35 实际按下未选中的“知识库”Tab 后抓取同状态基线与按压帧，导航区域 44,040 个变化像素全部位于该 Tab 的胶囊边界内，胶囊外变化像素为 0。指示器使用不透明主题预混色并置于图标层上方，关闭移动首帧旧图标穿透。
- 回归：全量 JVM 2037 项，0 failure/error、14 skip；Lint 0 Error/Fatal、403 warnings；AndroidTest Kotlin 编译与 `git diff --check` 通过。
- 复审：独立 Sol 三轮复审逐项关闭动画中断重置、双指示内容、设备时序覆盖、逐帧重组与图标穿透问题，最终 Critical/Important/Minor 均为 0，Task 4.1 代码级 GO。Luna 的静态视觉意见中，未把预览占位内容或用户明确选择的“仅选中项显示标签”误判为阻断；动效证据缺口已由 API 35 时序测试闭合。

### Material 3 总收敛 Task 5 锚定式附件动作验证

- RED：新增设备测试先因锚定菜单标签和行为缺失失败；未使用 `@Disabled`、注释或临时 stub。实现后又由独立复审发现顶栏点击穿透和静态截图双菜单风险，均通过生产路径与回归测试返修。
- GREEN：附件 `+` 原位过渡为关闭语义，单一 `AttachmentActionMenu` 使用真实按钮/输入栏坐标并位于整个聊天 `Scaffold` 上方；外部、顶栏和 Back 关闭不穿透，生成/导入会关闭并禁用菜单，首帧 fallback 随实测坐标到达自动切换。
- 设备：可见模拟器 API 31、35、36 的附件、聊天状态和无障碍组合各 29/29，均 0 failure/error/skip；API 36 系统动画缩放 0x 下附件测试 12/12，结束后已 readback 恢复为 `1/1/1`。真实 IME、2.0x、320dp 短窗口、RTL、48dp、禁用回调和顶栏拦截均有设备断言。
- 视觉：Screenshot 73/73；普通、英文 2.0x、中文 2.0x、紧凑视口、RTL 五张生产菜单 reference 已逐张检查，无重复菜单、裁切、重叠或模型/Token/发送区遮挡。紧凑静态图不冒充真实 IME，IME 证据来自设备测试。
- 回归：全量 JVM 2034 项，0 failure/error、14 skip；Lint 0 Error/Fatal、403 warnings、25 hints；AndroidTest Kotlin 编译与 `git diff --check` 通过。
- 复审：独立 Sol 与 Luna 最终均为 Critical 0、Important 0、Minor 0、Verdict PASS。

| 视口/条件 | 中文 | 英文 | 状态 |
|---|---|---|---|
| 360dp 紧凑手机 | PASS | PASS | Screenshot 基线覆盖双语紧凑布局，人工审阅通过。 |
| Pixel 7 竖屏 | PASS | PASS | 412×892 双语关键页面基线，人工审阅通过。 |
| Pixel 7 横屏 | PASS | PASS | 4 个关键横屏状态基线通过并逐张人工审阅；聊天输入区、文档保存入口、Provider 搜索及批量操作均可达。 |
| 系统字体 2.0x | PASS | PASS | 多个双语关键状态基线覆盖，人工审阅未发现关键控件不可达。 |
| 平板宽度/NavigationRail | PASS | PASS | 840×900 双语基线与 API 设备自适应导航测试通过。 |
| TalkBack 焦点、角色、动态播报 | PENDING | PENDING | API 31/35/36 无障碍 smoke 已通过；API 36 在 TalkBack 服务真实绑定时新增稳定输入名称、Prompt 编辑器名称、保存中 polite 与保存失败 assertive 语义断言，共 7/7。最终签名发行 APK 安装到真机后执行人工听觉与完整焦点遍历。 |
| IME 遮挡、滚动、返回、空/加载/错误态 | PASS | PASS | `ChatImeInteractionTest` 在 API 31/35/36 各 5/5；覆盖长消息尾部避让、键盘动画稳定、主动上滚不被夺回、关闭后 inset 清零与测试隔离。 |

关键页面/状态：欢迎与引导、Agent 首页、聊天空态/生成态/错误态/审批态、Provider、RAG、备份、设置、Prompt 编辑器、通知返回/停止。发现裁切、溢出、低对比、焦点陷阱、低于 48dp 的关键目标、键盘遮挡、无效按钮或不可恢复错误时，门禁为 FAIL。

## 7. 真实 API Smoke

2026-07-13 前一候选的四角色结果只作为历史协议基线。2026-07-21 已通过仅作用于当前测试进程的安全运行时输入，对提交 `a1dca2d8` 重新执行真实 Provider 门禁；凭据未写入源码、计划、handover、报告或 shell 命令参数。

| 项目 | 状态 | 安全要求 |
|---|---|---|
| Task 14 当前候选复验 | PASS | `--no-build-cache --rerun-tasks :app:realLlmIntegrationTest` 新鲜执行成功：1 test、0 failure/error/skip，29 tasks 全部执行，`BUILD SUCCESSFUL`（1m21s）。首次使用服务根路径真实请求失败；只读探测确认受保护模型端点位于 `/v1` 后，以 `/v1` 基址复跑通过。四个模型各请求一次，凭据未落盘。 |
| 2026-07-19 模型元数据候选复验 | PASS（历史） | `--no-build-cache --rerun-tasks :app:realLlmIntegrationTest` 当时新鲜执行成功：1 test、0 failure/error/skip，29 tasks 全部执行，`BUILD SUCCESSFUL`（47s）。四模型在同一 Router 中各请求一次；凭据未落盘。 |
| 多模态模型 | PASS | `MiniMax-M3`：精确显示为 `MiniMax M3`；图片输入、有效流式 payload 与 Done 终态通过，`delta.reasoning_content` 可解析。 |
| 高速文本模型 | PASS | `MiniMax-M2.7-highspeed`：精确显示为 `MiniMax M2.7 Highspeed`；有效流式 payload 与 Done 终态通过，未把未知推理证据强制改为支持。 |
| 高智力文本模型 | PASS | `deepseek-v4-flash`：精确显示为 `DeepSeek V4 Flash`；推理能力为 `SUPPORTED`，Chat endpoint 兼容性独立记录；有效推理 payload 与 Done 终态通过。 |
| 中等智力多模态模型 | PASS | `sensenova-6.7-flash-lite`：精确显示为 `SenseNova 6.7 Flash Lite`；图片输入、`delta.reasoning` 与 Done 终态通过，未知能力保持 `UNKNOWN`。 |

真实 API 只验证协议兼容、路由、流式终态和多模态输入；Release 仍保持 HTTPS-only，不能为内网测试放宽发行网络策略。

## 8. GitHub 治理与发布

| 项目 | 状态 | 待回填证据 |
|---|---|---|
| Task 14 应用候选推送 | PASS | 最终发布提交 `a7c94f03` 已推送；其在既有应用候选基础上增加托管模拟器 Launcher/SystemUI ANR 的严格一次性恢复，独立 Sol 复审 Critical/Important 0。 |
| tag provenance | PASS | `v0.2-beta` 为 SSH 签名 annotated tag，GitHub verification `valid`，目标为提交 `a7c94f03c321c3e8588649eac34c9a1bb4703296`。 |
| annotated 降级 | NOT-AUTHORIZED | 默认拒绝；如签名 tag 不可用，必须有用户明确授权与仓库变量记录。 |
| release environment | PASS | 四项签名 Secret 已存在；公开证书指纹与本地发行证书一致；1 分钟 wait timer 与 custom tag policy 已核验。未读取或记录 Secret 值。 |
| Android CI | PASS | 生产提交 [run 29877797382](https://github.com/Promenar/Nexara/actions/runs/29877797382) 首轮全绿；治理 HEAD `e7e7bd4f` 的 [run 29886387249](https://github.com/Promenar/Nexara/actions/runs/29886387249) 在 API 36 宿主 SystemUI ANR 后由失败 job 定向重跑闭合；当前远端 HEAD `3535735f` 的 [run 29893140602](https://github.com/Promenar/Nexara/actions/runs/29893140602) 四个 job 首轮全绿。最近一次完整 artifact 回读为 API 31 21 PASS + 1 DESIGN-SKIP、API 35/36 各 44 PASS + 1 DESIGN-SKIP；三档 `exit-code=0`、crash buffer 0 bytes。 |
| 远端发布候选签名入口 | PASS | workflow 对最终重建 APK 完成签名、R8、静态验真、API 35/36 冷安装与哈希回读。 |
| tag release workflow | PASS | [run 30420324239](https://github.com/Promenar/Nexara/actions/runs/30420324239)：发行输入、API 31/35/36 deviceTest、API 35/36 minifiedTest、签名构建、双 API 冷安装和 prerelease 发布全部成功。 |
| GitHub Release | PASS | [Nexara v0.2-beta](https://github.com/Promenar/Nexara/releases/tag/v0.2-beta) 为非草稿 prerelease；远端 APK 与 checksum 独立下载回读一致，SHA-256 `59d641b6b8f04a15bc9f4ce064a7a116df3def4efabf8231feb408612246fe9a`。旧 `v0.1-beta` Release 保留。 |

## 9. 已知限制确认

| 限制 | 状态 |
|---|---|
| 不兼容 `v0.1-beta` 数据，要求卸载旧版 | 已写入 README 与发行说明 |
| GGUF 不属于稳定能力，Release 不打包 | 已写入 README 与发行说明；最终 tag workflow 产物的真实扫描与 API 35/36 冷安装均 PASS |
| 后台生成不支持设备重启续传、多会话并行、无人值守队列或定时任务 | 已写入 README 与发行说明 |
| Release Provider/WebDAV 仅 HTTPS | 已写入 README 与发行说明 |
| 系统跟随、浅色、深色与 Android 12+ 动态色 | 已完成正式主题与富文本渲染门禁，并同步发行说明 |

## 10. 最终签字区

- 最终结论：`GO`
- 阻断项：无 prerelease 阻断项。物理真机 TalkBack 人工听觉、完整焦点遍历和更广泛 OEM/IME 体验作为 beta 后续验证项，不记为已完成。
- 放行人：Promenar
- 放行时间：2026-07-29
- 最终 commit：`a7c94f03c321c3e8588649eac34c9a1bb4703296`，由 SSH 签名 annotated tag `v0.2-beta` 固定，并通过受控 `NEXARA_RELEASE_COMMIT_SHA` 校验。
- Release URL：https://github.com/Promenar/Nexara/releases/tag/v0.2-beta
- APK SHA-256：`59d641b6b8f04a15bc9f4ce064a7a116df3def4efabf8231feb408612246fe9a`（GitHub 资产 digest、checksum 文件与独立下载计算三者一致）。
