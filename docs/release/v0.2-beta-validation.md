# Nexara v0.2-beta 发行验证记录

> 状态：**NO-GO / PENDING**
> 更新日期：2026-07-21
> 当前工作分支：`codex/md3-redesign`
> 最近一次已完成远端 Android CI 的应用候选：`bf6f87cc6143d3e6efe310cc95966ac638bd1f8c`（run `29408429117`；quality、API 31、API 35、API 36 最终全绿）

本文件是发行事实账本。只有本轮实际执行并可复核的结果才能标为 `PASS`；源码存在、测试文件编译、旧构建截图、计划或子 Agent 声明不能代替最终运行证据。

状态含义：

- `PASS`：当前工作树或明确记录的基线已经执行并取得可复核证据；
- `PENDING`：尚未执行、证据不完整或需要最终集成后重跑；
- `FAIL`：已执行但不满足门禁，必须修复后重跑；
- `N/A`：经产品范围明确排除，并记录理由。

## 1. 当前放行结论

| 门禁 | 状态 | 结论 |
|---|---|---|
| P0/P1 安全、数据与核心业务基线 | PASS | 2026-07-13 交接记录：1232 JVM、28 Android、Lint、Debug APK，以及备份/恢复多阶段流程通过；后续候选已完成 1632 JVM、36 张截图和 API 31/35/36 设备回归，该基线仍不替代最终签名包验真。 |
| P2 后台生成与 UI 专项实现 | PASS | 生成、通知、RAG、Prompt、资源树、设置、Provider、备份恢复与文档解析已完成确定性 JVM/截图/设备回归；API 31 minimum、API 35/36 full 均通过。 |
| P3 签名 R8 APK 与发行工程 | PENDING | 当前源码已使用 `clean` 与 `--rerun-tasks` 构建稳定证书签名 R8 APK，并闭合 R8、包身份、唯一签名者、登记证书、敏感/GGUF 扫描、checksum、zipalign 及 API 35/36 同哈希冷安装启动 smoke。签名候选真机人工业务验收、当前提交远端矩阵、GitHub 可验证 tag 与 tag release workflow 尚未完成。 |
| 商业级 UI/设备矩阵 | PENDING | 当前 90/90 Screenshot validation 通过且受影响 actual 已人工检查；可见 API 31/35/36 模拟器完成 Task 6 助手/会话搜索各 15/15、Task 7 模型选择器各 2/2、Task 8 设置首页/Provider/默认模型各 22/22、Task 9 检索设置各 6/6、Task 10 Provider/密钥组合各 20/20，API 35 既有流体导航快速切换、形变中点、反向连续性与 0x 动画缩放 6/6 保持通过。TalkBack 自动语义已闭合，人工听觉与完整焦点遍历仍须在当前签名发行 APK 真机安装后完成。 |
| GitHub tag 与 Release | PENDING | 尚未创建最终 tag 或上传 APK；当前结论为 NO-GO。 |

## 2. 已确证的专项测试

下列结果来自 2026-07-13 至 2026-07-19 的报告或主控独立复跑；最终发行仍须完成真机与远端门禁。

| 范围 | 命令/证据 | 结果 |
|---|---|---|
| 生成取消、Runner、Runtime、路由与流式背压 | 8 类定向 JVM 套件 | PASS：62 tests，0 failed，0 skipped；最终取消契约 9/9。 |
| Anthropic/OpenAI Responses 错误分类 | 两协议定向与组合回归 | PASS：定向 20/20；组合 58 tests，0 failed，4 个既有真实网络配置门禁 skip。 |
| RAG 移动、失败目标、重试与通知 | `RagViewModelTest`、`IndexingNoticeTest`、`RagHomeInteractionTest` | PASS：31 tests，0 failed/skipped/errors。 |
| 统一 Prompt 编辑器 | `UnifiedPromptEditorTest` + Screenshot validation + device E2E | PASS：JVM 5/5，截图基线已纳入 36/36 全量通过，AndroidTest 编译与设备主流程通过。 |
| DocEditor Material 3 第四阶段 | JVM/Lint/截图/设备/性能/独立复核 | PASS：1915 个 JVM 测试 0 failure/error、14 个既有条件 skip；56/56 截图；API 31/35/36 DocEditor 各 31/31；API 36 bulk 228 项 0 failure、3 个既有分阶段 skip，冷启动两方法各 1/1。两次性能 6/6 共 60 个样本无替换，最差 p95 35 ms、max 40 ms、PSS +12,194 KiB；终审 C0/C1/C2/C3=0、P0/P1/P2=0。 |
| Agent 首页 Material 3 连续列表 | 源码契约 + Screenshot validation + API 36 Compose | PASS：先以旧实现取得 7 项中 2 项预期 RED，再完成 9/9 契约；57/57 全量截图通过，普通英文与中文 2.0x 长标题/长描述/空描述均经人工审阅；API 36 的整行进入、菜单隔离、置顶语义、短滑不误删与空态入口共 6/6。 |
| 分层模型元数据与会话尾注 | Task 1-10 TDD/双复审 + 当前 Provider 门禁 | PASS：唯一解析入口、精确 ID、三态能力、Provider/用户覆盖迁移、引导探测、统一友好名称、AI 左侧尾注和离线目录供应链均已实现。定点模型 JVM 89/89；当前全量 JVM 1982 项，0 failure/error、14 skip；Lint 0 Error/Fatal；Screenshot 62/62；AndroidTest Kotlin 编译通过；API 31 minimum 与 API 35/36 full 设备脚本通过；当前四角色真实 Provider 流式验证通过。 |
| 空知识库、生成终态、流式淡入与设置密度 | 定向 TDD + 全量 JVM/截图/设备/双复审 | PASS：旧版孤儿索引任务只在缺失身份或文件确认不存在时清理，现代 reference 目标保留；失败/取消先发布展示终态；正常流式 chunk 即时显示并仅对尾部应用 140ms 淡入；设置与 Provider 使用连续无逐项卡片的 MD3 列表并保留 48dp 目标；数据库目录刷新不会用同 ID 迟到快照覆盖 Store 中较新的流式内容。当前全量 JVM、62 张 actual、API 31/35/36 设备矩阵通过，独立终审 Critical 0 / Important 0，剩余 3 个不阻断 Minor。 |
| 会话完整文档、导出与消息分支 | Task 1-6 TDD + 双复审 + 当前本地门禁 | PASS：输入栏 TXT/MD 以持久化快照作为完整上下文，不走 RAG；最终路由后执行 fail-closed Prompt 预算；导出可重新上传，稳定消息可创建独立分支。重试保留旧回复，失败/取消按生成前消息基线统一清理本轮全部 Assistant、工具结果与占位。定点 JVM 76 项；当前全量 JVM 2021 项，0 failure/error、14 skip；64/64 Screenshot；Lint 0 Error/Fatal；AndroidTest 编译；API 31/35/36 相关 Compose 各 8/8；Terra/Sol Critical/Important 0。本会话六项真实 Provider 变量未注入，未把历史 PASS 计为本候选复验。 |
| Material 3 总收敛施工前基线 | Task 1 当前候选 JVM/Screenshot/Lint/AndroidTest 编译 + actual 人工分类 | BASELINE：提交 `72f3f487` 上使用 `--no-build-cache --rerun-tasks` 新鲜执行：`:app:testDebugUnitTest` 为 2021 tests、0 failure/error、14 skip；Screenshot 64 张验证通过；Lint 0 Error/Fatal（401 warning、24 hint）；AndroidTest Kotlin 编译通过。人工检查 35 张设置、Provider、模型、RAG、资源与 Agent actual，并在第 6 节逐文件分类。全部仍只有深色基线，且二三级设置、模型选择器、上下文搜索、手机底部导航与平板 Rail 存在 `missing-baseline`。自动截图通过不构成新方案视觉 PASS。 |
| Material 3 总收敛 Task 6 助手会话与顶栏搜索 | TDD + JVM/Lint/Screenshot + API 31/35/36 + 双复审 | PASS：旧实现先取得 22 项中 7 项预期 RED；非空短结果搜索设备契约在 API 35 真实暴露两处位置恢复失败，改为稳定条目 ID、原始索引和像素偏移后闭合。当前全量 JVM 2046 项，0 failure/error、14 skip；Lint 0 Error/Fatal（406 warning）；79/79 Screenshot；AndroidTest Kotlin 编译；API 31/35/36 定向设备测试各 15/15。空态、双会话、长标题、2.0x 与搜索态 7 张受影响 actual 已逐张人工检查，TalkBack 置顶/删除自定义动作与返回优先级均有设备覆盖；Terra/Sol Critical 0、Important 0。 |
| Material 3 总收敛 Task 7 统一模型选择器 | TDD + JVM/Lint/Screenshot + API 31/35/36 + 双复审 | PASS：冻结 `ModelSelectionUiModel` 并通过 `ModelMetadataResolver.resolve(...)` 统一投影，通用面板与会话设置共用连续 Material 3 `ListItem`。全量 JVM 2085 项，0 failure/error、14 skip；Lint 0 Error/Fatal；81/81 Screenshot；AndroidTest Kotlin 编译；API 31/35/36 定向设备测试各 2/2。120 模型深色与长名称浅色 2.0x actual 已逐张检查，未知能力未制造支持；Terra/Sol Critical 0、Important 0。 |
| Material 3 总收敛 Task 8 设置首页与默认模型 | TDD + JVM/Lint/Screenshot + API 31/35/36 + 双复审 | PASS：设置首页收敛为六个连续分组，Provider 与四角色默认模型进入独立二级页；默认 Provider 禁止删除，额外 Provider 删除级联模型与预设；首页不做同步全量刷新，模型删除清理持久化元数据。全量 JVM 2100 项，0 failure/error、14 skip；Lint 0 Error/Fatal；85/85 Screenshot；AndroidTest Kotlin 编译；API 31/35/36 设置组合各 22/22。9 张首页/Provider/默认模型 actual 已逐张检查；Terra/Sol Critical 0、Important 0。 |
| Material 3 总收敛 Task 9 记忆、索引与检索设置 | TDD + JVM/Lint/Screenshot + API 31/35/36 + 双复审 | PASS：五个设置页迁移为无 Glass/嵌套卡片的连续 Material 3 参数表面，RAG/Search/Agent 状态所有者保持独立并使用原子 transform；全局 RAG 的状态发布与持久化由同一串行临界区保护，并有双线程字段更新契约。Tavily 保存/揭示/清除、Slider、Preset、域名、清理确认及单一 TalkBack 操作节点均有生产 Content 设备覆盖。全量 JVM 2106 项，0 failure/error、14 skip；Lint 0 Error/Fatal；90/90 Screenshot；AndroidTest Kotlin 编译；API 31/35/36 各 6/6。5 张 actual 已逐张检查；Terra/Sol Critical 0、Important 0。 |
| Material 3 总收敛 Task 10 Provider 表单与密钥交互 | TDD + JVM/Lint/Screenshot + API 31/35/36 + 双复审 | PASS：云端/本地 Provider 配置迁移为无大卡片的连续 Material 3 表单，协议使用标准单选行；测试/保存固定在响应式底部动作区，保存是唯一 filled 主动作。密钥掩码、揭示、离页清理、HTTPS 校验、本地模型恢复与测试取消合同保持不变；Testing/Success/Error 以本地化 `stateDescription` 稳定附着于测试按钮。全量 JVM 2108 项，0 failure/error、14 skip；Lint 0 Error/Fatal；90/90 Screenshot；AndroidTest Kotlin 编译；API 31/35/36 Provider 与密钥组合各 20/20。3 张 actual 已逐张检查；Terra/Sol Critical/Important/Minor 0。 |
| Release 日志源码契约 | `ReleaseLoggingSourceContractTest`、`PostProcessorTest` + minified/签名 APK 日志 | PASS：3/3 + 3/3；API 35/36 minifiedTest 冷启动与文档分享导入无 crash/ANR；最终签名 APK 在 API 35/36 的应用 PID 日志分别为 20/33 行，敏感字段扫描无命中。 |
| APK 验证器 | `python3 -m unittest scripts/tests/test_verify_release_apk.py` + 本地签名候选验真 | PASS：验证器既有 23 tests 保持通过；当前 18,315,976-byte 稳定证书签名 APK 的包身份、唯一签名者、登记证书、敏感内容、GGUF/本地推理制品、ZIP/体积与 SHA-256 全扫描通过。 |
| Metro 开发者 TUI | `node --test scripts/tests/nexara-metro-tui.test.js` | PASS：49 tests，0 fail；真实 adb 长时间运行不属于发行 APK 门禁。 |
| Release workflow 可靠性契约 | Python/Shell 契约、`bash -n`、Ruby YAML parse、独立复审 | PASS：fail-closed 动态正反例现为 18/18；冷安装脚本统一复用 APK 验证器，兼容 Build Tools 37 `V2 Signer` 且不再复制脆弱的证书解析；真实 LLM Gradle 任务禁止 up-to-date/构建缓存复用。全部 Python 合约、Metro TUI、设备脚本契约/语法与 6-job YAML 解析通过；独立复审 GO。`actionlint`、`shellcheck` 本机不可用，tag release workflow 的首次远端 run 仍为 PENDING。 |
| Room 关闭重开与分享索引重试 | `VectorizationQueueRoomTest`、`ShareImportMimeTypeTest`、API 36 Compose、release-equivalent minified 黑盒 | PASS：文件型 Room close/reopen 后恢复失败任务并复用同一 task id 重置为 pending；已知 MIME 紧凑显示单测通过；API 36 Compose 2/2，含 2.0x 字体四动作显示、触控高度与边界断言；混淆包 TXT 首次失败与再次重试均可观察且无 crash/ANR。 |
| Android 截图证据链 | `test-normalize-android-screencap-png.py`、minified 黑盒 8 张 `screen-*.png`、真实异常 PNG 像素比对 | PASS：标准库工具覆盖 PNG CRC、filter 0–4、坏输入拒绝、同路径原子替换；历史异常图归一化前后 1080×2400 RGB 逐像素一致且 alpha=255。最终 API 36 黑盒 8/8 PNG 均为 alpha=255、无 raw/current/previous/tmp 残留；DOCX indexed 与 TXT refailed 经无 alpha JPEG 视觉副本复核，画面、状态和动作完整。 |
| 真实 LLM 四角色兼容 | `:app:realLlmIntegrationTest` + Generic OpenAI-compatible 定向 JVM 测试 | PASS：同一 Router 中四个模型各请求一次；高速文本、高智力文本、多模态 MiniMax 与均衡多模态均取得有效流式 payload 和完成终态，图片输入通过，并兼容 `delta.reasoning`。取消传播由受控在途协议 Flow 和保持打开的 MockEngine SSE 响应独立验证，避免用可能已自然结束的真实高速响应作假阳性证明。真实任务禁用 up-to-date/构建缓存后新鲜执行成功（45s），凭据未落盘。 |
| 真机首轮缺陷回归 | `OnboardingAndroidEndToEndTest`、`AccessibilitySmokeTest`、`ProviderModelsAccessibilityTest` + API 36 手工入口验证 | PASS：21 项设备回归 0 failed，1 个仅供分阶段 force-stop 的用例按设计 skip；首聊成功后保持聊天页并可返回主界面，知识库/记忆设置/检索设置依次真实点击显示，聊天胶囊和模型管理 48dp 触控层及紧凑可见面均通过。 |
| 首次远端 Android CI | [run 29389720531](https://github.com/Promenar/Nexara/actions/runs/29389720531) | FAIL（已定位并在后续候选闭环）：JVM/Lint/构建成功，API 31 成功；API 35 因权限弹窗只搜索活动根而失败，API 36 因 onboarding 过早访问 ProviderManager 而失败。 |
| 第二次远端 Android CI | [run 29398804571](https://github.com/Promenar/Nexara/actions/runs/29398804571) | FAIL 后被后续 push 取消重跑：quality 与 API 31 成功；API 35 因测试真实旋转宿主 Activity 后 Compose root 被销毁而失败，API 36 因托管模拟器 System UI 无响应导致通知权限按钮不可达而失败。本机 API 36 完整设备链同提交通过；API 35 已改用 Compose 窗口/字体配置覆盖并连续 4 轮通过。 |
| 前一候选远端 Android CI | [run 29401903929](https://github.com/Promenar/Nexara/actions/runs/29401903929) | PASS：目标提交 `10d6761`；quality 18m09s、API 31 9m28s、API 35 11m03s、API 36 12m30s，四项全部成功。 |
| 最终应用候选远端 Android CI | [run 29408429117](https://github.com/Promenar/Nexara/actions/runs/29408429117) | PASS：目标提交 `bf6f87c`；quality 18m39s、API 31 9m53s、API 35 10m56s 成功。API 36 首轮被托管模拟器 `System UI isn't responding` 系统弹窗阻挡权限弹窗；保留截图/logcat 后只重跑失败作业，第二轮同一断言 13m03s 成功，最终 run conclusion 为 `success`。 |

## 3. 最终集成质量门禁

| 项目 | 状态 | 待回填证据 |
|---|---|---|
| clean 后全量 JVM | PASS | 当前工作树 JDK 21 完整 `:app:testDebugUnitTest`：2108 tests，0 failure/error、14 个既有条件 skip。历史 clean 基线仍保留，最终 tag workflow 需重新 clean 构建。 |
| Android Lint | PASS | `:app:lintDebug` 通过：0 Error/Fatal；最终报告位于 `native-ui/app/build/reports/`。 |
| Screenshot validation | PASS | 当前工作树 `:app:validateDebugScreenshotTest`：90/90 通过，0 skipped/failure/error；流体导航既有 actual、Task 6 助手/会话、Task 7 模型选择器、Task 8 设置首页/Provider/默认模型、Task 9 五个检索设置页以及 Task 10 Provider 表单的受影响 actual 已人工检查。静态截图不冒充动效连续视频，位移、形变与搜索位置恢复由设备时序断言覆盖。 |
| Android instrumentation | PASS | 当前发行脚本同源设备矩阵：API 31 minimum 18 项、API 35/36 full 各 37 项，均 0 failure/error，每台各 1 个设计内 force-stop checkpoint skip；覆盖通知权限与精确会话返回、新建会话入口、聊天主流程、onboarding、48dp/2.0x 无障碍、自适应导航、备份恢复、跨进程 relay、后台生成和真实 PDF/DOCX 解析。API 35 首轮真实暴露通知发布异步竞态，E2E 改为有界等待 active notification 后两档 full 矩阵重跑通过；当前分支远端矩阵待最终 tag workflow。 |
| Debug APK | PASS | 当前工作树 `--rerun-tasks :app:assembleDebug` 新鲜执行通过；最终 tag workflow 仍须重新 clean 构建发行包。 |
| Release APK | PASS | 当前源码于 2026-07-21 使用稳定证书执行 `clean` 后 `:app:assembleRelease --rerun-tasks`；`nexara-v0.2-beta.apk` 为 18,316,200 bytes，SHA-256 `0114f525b8be881967f3389386505ebd11d2f611e1bf1149e8ada512610460ba`，并完成 fail-closed 验真、16 KiB zipalign 与 API 35/36 同哈希冷安装启动 smoke。GitHub Release 仍须由 tag workflow clean 重建并复验。 |

建议最终命令（以实际 Gradle task 为准）：

```bash
cd native-ui
./gradlew --no-daemon clean \
  :app:testDebugUnitTest \
  :app:lintDebug \
  :app:validateDebugScreenshotTest \
  :app:assembleDebug \
  :app:assembleRelease
```

## 4. 签名、R8 与 APK 安全

| 项目 | 状态 | 待回填证据 |
|---|---|---|
| 稳定发行证书 | PASS | 本地发行证书有效期 2025-12-29 至 2053-05-16；SHA-256 `00:BE:4C:DD:83:78:AA:FB:D7:0E:BC:43:E9:71:52:37:91:CC:07:DE:EA:D6:31:E0:6D:CF:15:5D:EE:EE:38:02`，与 GitHub `release` environment 登记值逐字节一致。未记录别名或密码。 |
| R8 mapping | PASS | 当前候选 `mapping.txt` 99,271,231 bytes；保留为受控构建证据，不作为公开 Release 资产。 |
| R8 seeds/usage/configuration | PASS | 当前候选 `seeds.txt` 773,829 bytes、`usage.txt` 11,227,490 bytes、`configuration.txt` 67,338 bytes。 |
| APK 包身份 | PASS | `com.promenar.nexara.native`、versionCode 2、versionName `0.2-beta`。 |
| 单一签名者/证书匹配 | PASS | 统一 APK 验证器兼容 Build Tools 37 `V2 Signer`，确认唯一签名者且证书与登记 SHA-256 一致。 |
| ZIP/体积/敏感内容扫描 | PASS | `verify-release-apk.py` 对当前 18,315,976-byte APK 验证通过；低于 50 MiB，ZIP、跨块敏感内容及 GGUF/本地推理制品扫描通过。 |
| zipalign | PASS | `zipalign -c -P 16 4` 通过。 |
| SHA-256 | PASS | 当前候选 `f54fb3f457637175544a2a2401cddd3099e6243b20411b299e53dfb14c9d457a`；由统一 APK 验证器输出，并再次以 `shasum -a 256` 复核一致。 |
| Release logcat | PASS | 当前同哈希候选在 API 35/36 冷启动后均保持进程存活且 crash buffer 为空；启动耗时分别为 253ms 和 374ms。最终 tag workflow 产物须重新执行相同日志扫描。 |
| GGUF/llama/ggml 排除 | PASS | 真实签名 APK 扫描通过，未打包 GGUF、llama 或 ggml 制品。 |

## 5. 冷安装与 minified 业务验真

| 场景 | API 35 | API 36 | 证据要求 |
|---|---|---|---|
| 卸载旧包、侧载签名 APK、冷启动、无 crash/ANR | PASS | PASS | 当前 SHA-256 `f54fb3f...d457a` 的 18,315,976-byte 候选在可见 API 35/36 模拟器完成无旧包状态确认、安装、明确 launcher Activity 冷启动和 5 秒应用级 crash buffer 观察；启动耗时分别为 253ms 和 374ms，两台应用进程均存活且 crash buffer 为空。该 smoke 不替代后续真机业务 E2E。 |
| Room 新建、强停、重启读取 | PENDING | PENDING | minified APK 实际数据。 |
| 中英文 onboarding 至首聊 | PENDING | PENDING | 截图 + UI tree/测试报告。 |
| Provider 表单与模型选择 | PENDING | PENDING | 不使用真实 Key 的黑盒流程。 |
| 无 Key 备份 round-trip | PENDING | PENDING | 导出、强停、恢复、no-replay。 |
| 含 Key 加密备份 round-trip | PENDING | PENDING | 仅用脱敏测试凭据，不在日志/截图暴露内容。 |
| DOCX/PDF 解析 | PASS | PASS | API 35/36 `minifiedTest` APK 使用真实 PDF/DOCX fixture 完成分享导入，覆盖 R8/反射边界；证据位于对应 `artifacts/android-minified-blackbox-*` 目录。 |
| RAG 导入/索引/重试 | PASS | PASS | API 35/36 release-equivalent minifiedTest 均使用真实 TXT fixture 完成导入，观察到索引失败、启用 Retry、点击后再次失败并可再次重试；API 35 证据目录为 `artifacts/android-minified-blackbox-api-35-arm64-rag-room-20260715-final/`，exit=0、crash buffer 为空。 |
| 后台/锁屏/旋转/停止/返回 | PENDING | PENDING | 当前任务持续、终态唯一、通知返回准确会话。 |

当前 workflow 的签名 APK smoke 只覆盖校验、冷安装、启动、前台存活与 crash/ANR；在 minified 关键业务流程补齐前，不得把该 smoke 描述为完整发行 E2E。

## 6. UI、视觉与无障碍矩阵

所有截图必须来自最终集成后的当前构建，逐张人工审阅。旧安装包或历史 `/tmp` 截图只能作线索，不能作为本版 PASS 证据。

截至 2026-07-21，当前 90 张 Screenshot validation 全部通过，主控已检查本轮新增及受影响 actual，覆盖完整文档中英文、大字体、长文件名、聊天 ready/reasoning/stream/error、助手/会话空态与搜索态、120 模型目录、长模型名和未知能力，以及设置首页、Provider、四角色默认模型、五个检索设置页、Provider 表单、知识库空态和流体手机导航等基线。API 31/35/36 的 Task 6 助手/会话各 15/15、Task 7 模型选择器各 2/2、Task 8 设置组合各 22/22、Task 9 检索设置各 6/6、Task 10 Provider 与密钥组合各 20/20，API 35 流体导航 6/6；当前签名 release 已在 API 35/36 完成同哈希冷安装、冷启动、进程存活和 crash buffer 复验。流式输出与导航动效的连续性由时序契约和设备状态覆盖，静态截图不冒充连续视频。人工听觉与全焦点遍历安排在最终签名发行 APK 真机安装后验收。

### Material 3 总收敛 Task 1 actual 分类

以下标识对应 `native-ui/app/build/outputs/screenshotTest-results/preview/debug/rendered/` 内文件名的测试函数前缀。分类只冻结迁移目标，不表示新视觉已经通过。

- `keep`（6）：`agentHubEmptyLargeFontReleasePreview`、`agentHubPopulatedLargeFontReleasePreview`、`agentHubReleasePreview`、`ragDetailsKgLongLandscapeReleasePreview`、`ragDetailsRetrievedPhoneReleasePreview`、`ragDetailsWebLargeFontReleasePreview`。
- `migrate-form`（4）：`providerFormAddReleasePreview`、`providerFormEditActionsLargeFontReleasePreview`、`providerFormLocalFailureReleasePreview`、`providerMaskedKeyReleasePreview`。
- `migrate-list`（9，设置/模型）：`onboardingModelListReleasePreview`、`providerModelsChineseLandscapeReleasePreview`、`providerModelsReleasePreviewChineseLargeFont`、`providerModelsReleasePreviewEnglish`、`userSettingsAppChineseCompactLargeFontReleasePreview`、`userSettingsAppChineseTabletReleasePreview`、`userSettingsProviderEmptyChineseLandscapeLargeFontReleasePreview`、`userSettingsProviderEmptyChineseLargeFontReleasePreview`、`userSettingsProviderLargeFontReleasePreview`。
- `migrate-list`（12，RAG）：`ragFilesPanelDeepTreeLargeFontReleasePreview`、`ragFolderContentLandscapeReleasePreview`、`ragFolderContentPhoneReleasePreview`、`ragFolderSelectedLargeFontReleasePreview`、`ragHomeDocumentsLandscapeReleasePreview`、`ragHomeDocumentsReleasePreviewEnglish`、`ragHomeMemoryChineseLargeFontReleasePreview`、`ragHomeMemoryContentLargeFontReleasePreview`、`ragHomeMemoryContentPhoneReleasePreview`、`ragHomeMemoryDeleteConfirmationReleasePreview`、`ragHomeMemoryExpandedPhoneReleasePreview`、`ragHomeSelectedDocumentsPhoneReleasePreview`。
- `migrate-list`（4，资源管理）：`resourceExplorerFilesImportEnglishReleasePreview`、`resourceExplorerRecycleDeleteConfirmChineseLargeFontReleasePreview`、`resourceExplorerRecycleFailureLandscapeReleasePreview`、`resourceExplorerRecycleRunningChineseLargeFontReleasePreview`。
- `theme-blocked`：上述 35 张仅覆盖现有深色方案；浅色、系统跟随和动态色均无 current actual，不得用这些图片批准主题迁移。
- `missing-baseline`：二三级设置首页、记忆设置、检索设置、主题选择、通用模型选择器、会话上下文搜索、手机底部导航和大屏 `NavigationRail` 尚无本轮专用基线，按后续 Task 补齐。

### Material 3 总收敛 Task 2 主题基础验证

- RED：定点测试在 `compileDebugUnitTestKotlin` 因缺少冻结的主题枚举、偏好模型、Store、浅色 ColorScheme、Application 所有者与 Settings 委托接口而失败；未使用 `@Disabled`、注释或临时 stub。
- GREEN：`ThemePreferenceStoreTest`、`NexaraThemeTokenTest`、`SettingsViewModelTest`、`BackupPreferenceInventoryTest` 共 28 项，0 failure/error/skip；Debug Kotlin 与 AndroidTest Kotlin 编译通过。
- 回归：全量 JVM 2028 项，0 failure/error、14 skip；Screenshot 64/64；Lint 0 Error/Fatal；`git diff --check` 通过。
- 复审：独立 Sol 复审 Critical 0、Important 0；保留 1 个非阻断 Minor，即系统栏与主题分支尚无设备级直接验证，将在后续主题 UI/设备任务闭合。
- 阶段边界：当前只验证主题状态、持久化、备份白名单和双 ColorScheme。浅色、系统跟随、动态色的 current actual 仍为 PENDING，不得据此批准全站主题迁移。

### Material 3 总收敛 Task 3 共享原语验证

- RED：定点 JVM 9 项中 6 项只因 `NexaraSettingsSection`、`NexaraSearchTopBar` 缺失而失败；AndroidTest 编译只因新 Top Bar 尚不存在而失败，未使用 disabled、注释或 stub。
- GREEN：定点 JVM 9/9；全量 JVM 2034 项，0 failure/error、14 skip；AndroidTest Kotlin 编译通过。
- 设备：可见 Pixel 7 API 36 上新增两个方法 2/2，通过 2.0x 长标题、48dp、常态 actions、自动焦点、文本输入、IME Search、清除、两级系统 Back、0x reduced-motion 两帧完成以及分隔线与未合并正文 bounds 对齐。
- 视觉：Screenshot 64/64；唯一变化为 `backupSettingsReleasePreview` 的分组标题从旧 10sp 全大写静态样式迁移到 Material `labelLarge`，主控逐张对照无裁切后更新 reference。Lint 0 Error/Fatal、403 warnings、24 hints。
- 复审：独立 Terra 首轮提出 1 个 Important 测试证据缺口；补齐真实设备交互与 bounds 后复审 Critical/Important/Minor 均为 0，Verdict Approve。
- 阶段边界：共享原语已可供后续页面消费，但大部分设置/搜索页面尚未迁移，不能把本任务描述为全站视觉完成。

### Material 3 总收敛 Task 4 自适应导航验证

- RED：AndroidTest 与 Screenshot 测试源先编译通过；API 36 导航 4 项中新增两项因缺少三个标准 Tab 标签失败，旧宽度断点/960dp 约束继续通过；四张新预览因 reference 缺失使 Screenshot 68 项中 4 项失败。
- GREEN：手机使用单层 tonal `Surface + NavigationBar + NavigationBarItem`，大屏保留 `NavigationRailItem`；移除旧自绘 Row、glow、`drawBehind`、`setShadowLayer` 与 scale-only feedback。
- 设备：可见 Pixel 7 API 36 `AdaptiveNavigationTest` 4/4；覆盖 599/600dp 断点、三项 Role.Tab/selected/48dp、点击切换、Rail 唯一性，并真实滚动 50 项列表到末项验证其底边不越过导航坞顶边。
- 视觉：Screenshot 68/68；手机/平板、深色/浅色四张 actual 逐张检查，手机保持克制外边距和 Material 选中指示，平板 Rail 稳定，无裁切、标签溢出或低对比。
- 回归：全量 JVM 2034 项，0 failure/error、14 skip；Lint 0 Error/Fatal、403 warnings、24 hints；AndroidTest/ScreenshotTest Kotlin 编译与 `git diff --check` 通过。
- 复审：独立 Luna Critical/Important 为 0，Verdict Approve；其滚动末项 Minor 已补真实设备断言关闭，占位内容截图仅承担隔离导航视觉的职责。

### Material 3 总收敛 Task 4.1 流体胶囊导航返修验证

- RED：`FluidNavigationMotionTest` 首次进入 `compileDebugUnitTestKotlin`，只因缺少 `fluidNavigationIndicatorScale` 与 `fluidNavigationTargetIndex` 失败；未提前实现生产接口，未使用 disabled、注释或临时 stub。
- GREEN：手机端改为 64dp 全胶囊 tonal Surface 与单一 48dp 移动指示器，选中项展示图标和标签、未选中项展示图标；弹簧位移叠加横向拉伸/纵向收缩，快速反向切换从当前形变接续，0x 动画缩放直接到终态。每个 Tab 的原生 bounded ripple 由 `CircleShape` 裁剪，不再暴露矩形槽位；600dp 及以上 Rail 保持不变。
- 设备：可见 Pixel 7 API 35 `AdaptiveNavigationTest` 6/6；覆盖 599/600dp 断点、三项 Role.Tab/selected/48dp、内容避让、快速连点、形变中点、反向切换首帧连续性及 `MotionDurationScale=0`。
- 视觉：Screenshot 75/75；深色、浅色、英文 2.0x、紧凑横屏四张手机 actual 逐张检查，导航坞高度、选中标签、长字体、系统 Insets 均无裁切或重叠。API 35 实际按下未选中的“知识库”Tab 后抓取同状态基线与按压帧，导航区域 44,040 个变化像素全部位于该 Tab 的胶囊边界内，胶囊外变化像素为 0。指示器使用不透明主题预混色并置于图标层上方，关闭移动首帧旧图标穿透。
- 回归：全量 JVM 2037 项，0 failure/error、14 skip；Lint 0 Error/Fatal、403 warnings；AndroidTest Kotlin 编译与 `git diff --check` 通过。
- 复审：独立 Sol 三轮复审逐项关闭动画中断重置、双指示内容、设备时序覆盖、逐帧重组与图标穿透问题，最终 Critical/Important/Minor 均为 0，Task 4.1 代码级 GO。Luna 的静态视觉意见中，未把预览占位内容或用户明确选择的“仅选中项显示标签”误判为阻断；动效证据缺口已由 API 35 时序测试闭合。

### Material 3 总收敛 Task 5 锚定式附件动作验证

- RED：新增设备测试先因锚定菜单标签和行为缺失失败；未使用 `@Disabled`、注释或临时 stub。实现后又由独立复审发现顶栏点击穿透和静态截图双菜单风险，均通过生产路径与回归测试返修。
- GREEN：附件 `+` 原位过渡为关闭语义，单一 `AttachmentActionMenu` 使用真实按钮/输入栏坐标并位于整个聊天 `Scaffold` 上方；外部、顶栏和 Back 关闭不穿透，生成/导入会关闭并禁用菜单，首帧 fallback 随实测坐标到达自动切换。
- 设备：可见模拟器 API 31、35、36 的附件、聊天状态和无障碍组合各 29/29，均 0 failure/error/skip；API 36 系统动画缩放 0x 下附件测试 12/12，结束后已 readback 恢复为 `1/1/1`。真实 IME、2.0x、320dp 短窗口、RTL、48dp、禁用回调和顶栏拦截均有设备断言。
- 视觉：Screenshot 73/73；普通、英文 2.0x、中文 2.0x、紧凑视口、RTL 五张生产菜单 reference 已逐张检查，无重复菜单、裁切、重叠或模型/Token/发送区遮挡。紧凑静态图不冒充真实 IME，IME 证据来自设备测试。
- 回归：全量 JVM 2034 项，0 failure/error、14 skip；Lint 0 Error/Fatal、403 warnings、25 hints；AndroidTest Kotlin 编译与 `git diff --check` 通过。
- 复审：独立 Sol 与 Luna 最终均为 Critical 0、Important 0、Minor 0、Verdict PASS。

| 视口/条件 | 中文 | 英文 | 状态 |
|---|---|---|---|
| 360dp 紧凑手机 | PASS | PASS | Screenshot 基线覆盖双语紧凑布局，人工审阅通过。 |
| Pixel 7 竖屏 | PASS | PASS | 412×892 双语关键页面基线，人工审阅通过。 |
| Pixel 7 横屏 | PASS | PASS | 4 个关键横屏状态基线通过并逐张人工审阅；聊天输入区、文档保存入口、Provider 搜索及批量操作均可达。 |
| 系统字体 2.0x | PASS | PASS | 多个双语关键状态基线覆盖，人工审阅未发现关键控件不可达。 |
| 平板宽度/NavigationRail | PASS | PASS | 840×900 双语基线与 API 设备自适应导航测试通过。 |
| TalkBack 焦点、角色、动态播报 | PENDING | PENDING | API 31/35/36 无障碍 smoke 已通过；API 36 在 TalkBack 服务真实绑定时新增稳定输入名称、Prompt 编辑器名称、保存中 polite 与保存失败 assertive 语义断言，共 7/7。最终签名发行 APK 安装到真机后执行人工听觉与完整焦点遍历。 |
| IME 遮挡、滚动、返回、空/加载/错误态 | PASS | PASS | `ChatImeInteractionTest` 在 API 31/35/36 各 5/5；覆盖长消息尾部避让、键盘动画稳定、主动上滚不被夺回、关闭后 inset 清零与测试隔离。 |

关键页面/状态：欢迎与引导、Agent 首页、聊天空态/生成态/错误态/审批态、Provider、RAG、备份、设置、Prompt 编辑器、通知返回/停止。发现裁切、溢出、低对比、焦点陷阱、低于 48dp 的关键目标、键盘遮挡、无效按钮或不可恢复错误时，门禁为 FAIL。

## 7. 真实 API Smoke

2026-07-13 前一候选的四角色结果只作为历史协议基线。2026-07-19 已对当前分层模型元数据候选重新执行真实 Provider 门禁，以下结果均来自本次新鲜请求；未复用历史测试结果或 Gradle 缓存。

| 项目 | 状态 | 安全要求 |
|---|---|---|
| 当前模型元数据候选复验 | PASS | `--no-build-cache --rerun-tasks :app:realLlmIntegrationTest` 新鲜执行成功：1 test、0 failure/error/skip，29 tasks 全部执行，`BUILD SUCCESSFUL`（47s）。四模型在同一 Router 中各请求一次；凭据只注入当前 Gradle 子进程，未写入仓库、报告或命令参数。 |
| 多模态模型 | PASS | `MiniMax-M3`：精确显示为 `MiniMax M3`；图片输入、有效流式 payload 与 Done 终态通过，`delta.reasoning_content` 可解析。 |
| 高速文本模型 | PASS | `MiniMax-M2.7-highspeed`：精确显示为 `MiniMax M2.7 Highspeed`；有效流式 payload 与 Done 终态通过，未把未知推理证据强制改为支持。 |
| 高智力文本模型 | PASS | `deepseek-v4-flash`：精确显示为 `DeepSeek V4 Flash`；推理能力为 `SUPPORTED`，Chat endpoint 兼容性独立记录；有效推理 payload 与 Done 终态通过。 |
| 中等智力多模态模型 | PASS | `sensenova-6.7-flash-lite`：精确显示为 `SenseNova 6.7 Flash Lite`；图片输入、`delta.reasoning` 与 Done 终态通过，未知能力保持 `UNKNOWN`。 |

真实 API 只验证协议兼容、路由、流式终态和多模态输入；Release 仍保持 HTTPS-only，不能为内网测试放宽发行网络策略。

## 8. GitHub 治理与发布

| 项目 | 状态 | 待回填证据 |
|---|---|---|
| 最终应用候选已推送 | PASS | Material 3 统一模型选择器候选已提交为 `6912952724afbfa20ae86c7105440d78632dba26`，并推送至 `origin/codex/md3-redesign`。本地 staged diff 与敏感路径复核通过；未纳管 `artifacts/`、`secure_env/` 或 `.nexara-workspace-*`。 |
| tag provenance | PENDING | tag object、目标 commit ancestry、GitHub verification；不允许 lightweight tag。 |
| annotated 降级 | PENDING | 默认拒绝；如签名 tag 不可用，必须有用户明确授权与仓库变量记录。 |
| release environment | PASS | 四项签名 Secret 已存在；公开证书指纹与本地发行证书一致；1 分钟 wait timer 与 custom tag policy 已核验。未读取或记录 Secret 值。 |
| Android CI | PENDING | 前一候选 [run 29408429117](https://github.com/Promenar/Nexara/actions/runs/29408429117) 在 `bf6f87c` 上最终 quality、API 31、API 35、API 36 全部成功；当前候选 `69129527` 已推送，但其远端 quality/API 31/35/36 矩阵尚未完成核验。 |
| tag release workflow | PENDING | 远端 run URL、每个 job 结论。 |
| GitHub Release | PENDING | prerelease URL、APK 与 checksum 资产、远端回读哈希一致。 |

## 9. 已知限制确认

| 限制 | 状态 |
|---|---|
| 不兼容 `v0.1-beta` 数据，要求卸载旧版 | 已写入 README 与发行说明 |
| GGUF 不属于稳定能力，Release 不打包 | 已写入 README 与发行说明；本地稳定证书签名 APK 真实扫描 PASS，tag workflow 产物仍须复验 |
| 后台生成不支持设备重启续传、多会话并行、无人值守队列或定时任务 | 已写入 README 与发行说明 |
| Release Provider/WebDAV 仅 HTTPS | 已写入 README 与发行说明 |
| 仅深色主题为正式主题 | 已写入发行说明 |

## 10. 最终签字区

- 最终结论：`NO-GO / PENDING`
- 阻断项：签名候选真机 TalkBack 人工听觉/全焦点遍历与核心业务体验、当前提交远端 CI、GitHub 可验证 tag、tag release workflow 与 GitHub Release 尚未完成。
- 放行人：PENDING
- 放行时间：PENDING
- 最终 commit：PENDING
- Release URL：PENDING
- APK SHA-256：PENDING
