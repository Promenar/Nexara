# Nexara v0.2-beta 发行验证记录

> 状态：**NO-GO / PENDING**
> 更新日期：2026-07-15
> 分支：`codex/v0.2-beta`
> 最近一次已完成远端 Android CI 的代码候选：`10d676115530aa387ad0644b6c3a9368a3736a30`（quality、API 31、API 35、API 36 全绿；当前协议兼容修复将由后续新 run 复验）

本文件是发行事实账本。只有本轮实际执行并可复核的结果才能标为 `PASS`；源码存在、测试文件编译、旧构建截图、计划或子 Agent 声明不能代替最终运行证据。

状态含义：

- `PASS`：当前工作树或明确记录的基线已经执行并取得可复核证据；
- `PENDING`：尚未执行、证据不完整或需要最终集成后重跑；
- `FAIL`：已执行但不满足门禁，必须修复后重跑；
- `N/A`：经产品范围明确排除，并记录理由。

## 1. 当前放行结论

| 门禁 | 状态 | 结论 |
|---|---|---|
| P0/P1 安全、数据与核心业务基线 | PASS | 2026-07-13 交接记录：1232 JVM、28 Android、Lint、Debug APK，以及备份/恢复多阶段流程通过；后续候选已完成 1632 JVM、36 张截图和 API 31/35/36 设备回归，该基线仍不替代最终签名包验真。 |
| P2 后台生成与 UI 专项实现 | PASS | 生成、通知、RAG、Prompt、资源树、设置、Provider、备份恢复与文档解析已完成确定性 JVM/截图/设备回归；API 31 minimum、API 35/36 full 均通过。 |
| P3 签名 R8 APK 与发行工程 | PENDING | API 35/36 的 release-equivalent minifiedTest 业务回归、分支 Android CI 及独立复审均已通过。稳定证书签名 R8 APK、mapping/缩减证据、包身份、签名、安全扫描、checksum、zipalign、API 35/36 冷安装与启动 smoke 已闭合；签名候选真机人工业务验收、GitHub 可验证 tag 与 tag release workflow 尚未完成。 |
| 商业级 UI/设备矩阵 | PENDING | 当前 36/36 Screenshot validation、API 31/35/36 欢迎布局、自适应导航、IME 5/5 与无障碍 smoke 已通过；36 张基线已逐张人工审阅。分享表单的 DOCX 长 MIME 窄屏换行缺陷已改为紧凑标签，四动作区改为可换行布局，并在 API 36 的常规画面与 2.0x 字体 Compose 测试中复验。TalkBack 真实绑定下的自动语义门禁已通过；人工听觉与完整焦点遍历将在最终签名发行 APK 安装到真机后完成。 |
| GitHub tag 与 Release | PENDING | 尚未创建最终 tag 或上传 APK；当前结论为 NO-GO。 |

## 2. 已确证的专项测试

下列结果来自 2026-07-13 至 2026-07-15 的本轮报告或主控独立复跑；最终发行仍须完成签名与远端门禁。

| 范围 | 命令/证据 | 结果 |
|---|---|---|
| 生成取消、Runner、Runtime、路由与流式背压 | 8 类定向 JVM 套件 | PASS：62 tests，0 failed，0 skipped；最终取消契约 9/9。 |
| Anthropic/OpenAI Responses 错误分类 | 两协议定向与组合回归 | PASS：定向 20/20；组合 58 tests，0 failed，4 个既有真实网络配置门禁 skip。 |
| RAG 移动、失败目标、重试与通知 | `RagViewModelTest`、`IndexingNoticeTest`、`RagHomeInteractionTest` | PASS：31 tests，0 failed/skipped/errors。 |
| 统一 Prompt 编辑器 | `UnifiedPromptEditorTest` + Screenshot validation + device E2E | PASS：JVM 5/5，截图基线已纳入 36/36 全量通过，AndroidTest 编译与设备主流程通过。 |
| Release 日志源码契约 | `ReleaseLoggingSourceContractTest`、`PostProcessorTest` + minified/签名 APK 日志 | PASS：3/3 + 3/3；API 35/36 minifiedTest 冷启动与文档分享导入无 crash/ANR；最终签名 APK 在 API 35/36 的应用 PID 日志分别为 20/33 行，敏感字段扫描无命中。 |
| APK 验证器 | `python3 -m unittest scripts/tests/test_verify_release_apk.py` + 本地签名候选验真 | PASS：23 tests，OK；兼容 Android 16 `aapt` 与 Build Tools 37 `apksigner` 输出，重复包身份行或现代格式缺失 signer 计数时均 fail-closed。17,971,235-byte 稳定证书签名 APK 的包身份、唯一签名者、登记证书、敏感内容、GGUF/本地推理制品、ZIP/体积与 SHA-256 全扫描通过。 |
| Metro 开发者 TUI | `node --test scripts/tests/nexara-metro-tui.test.js` | PASS：49 tests，0 fail；真实 adb 长时间运行不属于发行 APK 门禁。 |
| Release workflow 可靠性契约 | Python/Shell 契约、`bash -n`、Ruby YAML parse、独立复审 | PASS：fail-closed 动态正反例现为 18/18；冷安装脚本统一复用 APK 验证器，兼容 Build Tools 37 `V2 Signer` 且不再复制脆弱的证书解析；真实 LLM Gradle 任务禁止 up-to-date/构建缓存复用。全部 Python 合约、Metro TUI、设备脚本契约/语法与 6-job YAML 解析通过；独立复审 GO。`actionlint`、`shellcheck` 本机不可用，tag release workflow 的首次远端 run 仍为 PENDING。 |
| Room 关闭重开与分享索引重试 | `VectorizationQueueRoomTest`、`ShareImportMimeTypeTest`、API 36 Compose、release-equivalent minified 黑盒 | PASS：文件型 Room close/reopen 后恢复失败任务并复用同一 task id 重置为 pending；已知 MIME 紧凑显示单测通过；API 36 Compose 2/2，含 2.0x 字体四动作显示、触控高度与边界断言；混淆包 TXT 首次失败与再次重试均可观察且无 crash/ANR。 |
| Android 截图证据链 | `test-normalize-android-screencap-png.py`、minified 黑盒 8 张 `screen-*.png`、真实异常 PNG 像素比对 | PASS：标准库工具覆盖 PNG CRC、filter 0–4、坏输入拒绝、同路径原子替换；历史异常图归一化前后 1080×2400 RGB 逐像素一致且 alpha=255。最终 API 36 黑盒 8/8 PNG 均为 alpha=255、无 raw/current/previous/tmp 残留；DOCX indexed 与 TXT refailed 经无 alpha JPEG 视觉副本复核，画面、状态和动作完整。 |
| 真实 LLM 四角色兼容 | `:app:realLlmIntegrationTest` + Generic OpenAI-compatible 定向 JVM 测试 | PASS：同一 Router 中四个模型各请求一次；高速文本、高智力文本、多模态 MiniMax 与均衡多模态均取得有效流式 payload 和完成终态，图片输入通过，并兼容 `delta.reasoning`。取消传播由受控在途协议 Flow 和保持打开的 MockEngine SSE 响应独立验证，避免用可能已自然结束的真实高速响应作假阳性证明。真实任务禁用 up-to-date/构建缓存后新鲜执行成功（45s），凭据未落盘。 |
| 首次远端 Android CI | [run 29389720531](https://github.com/Promenar/Nexara/actions/runs/29389720531) | FAIL（已定位并在后续候选闭环）：JVM/Lint/构建成功，API 31 成功；API 35 因权限弹窗只搜索活动根而失败，API 36 因 onboarding 过早访问 ProviderManager 而失败。 |
| 第二次远端 Android CI | [run 29398804571](https://github.com/Promenar/Nexara/actions/runs/29398804571) | FAIL 后被后续 push 取消重跑：quality 与 API 31 成功；API 35 因测试真实旋转宿主 Activity 后 Compose root 被销毁而失败，API 36 因托管模拟器 System UI 无响应导致通知权限按钮不可达而失败。本机 API 36 完整设备链同提交通过；API 35 已改用 Compose 窗口/字体配置覆盖并连续 4 轮通过。 |
| 最终候选远端 Android CI | [run 29401903929](https://github.com/Promenar/Nexara/actions/runs/29401903929) | PASS：目标提交 `10d6761`；quality 18m09s、API 31 9m28s、API 35 11m03s、API 36 12m30s，四项全部成功。 |

## 3. 最终集成质量门禁

| 项目 | 状态 | 待回填证据 |
|---|---|---|
| clean 后全量 JVM | PASS | JDK 21 clean 基线 `:app:testDebugUnitTest --rerun-tasks`：1632 tests，0 failure/error，14 skipped；随后 JDK 17-like clean 发行门禁再次通过。当前最终候选再次执行完整 `:app:testDebugUnitTest`：1641 tests，0 failure/error，14 skipped。 |
| Android Lint | PASS | `:app:lintDebug` 通过：0 Error/Fatal；412 条 warning 均未阻断构建，最终报告位于 `native-ui/app/build/reports/`。 |
| Screenshot validation | PASS | `:app:validateDebugScreenshotTest`：36/36 通过；新增 4 张横屏基线，资源列表改为固定时钟；文档冲突大字体状态在优先保障两个 48dp 操作后更新 1 张预期基线，并再次全量通过。 |
| Android instrumentation | PASS | 本机 API 31 minimum、API 35/36 full、三版本 IME 5/5 与 API 36 TalkBack/DocEditor 均已通过；`10d6761` 上 API 35 full exit=0，覆盖通知拒绝/允许、通知回跳、欢迎页压力、备份恢复进程重启、RAG 文档解析与后台服务。远端最终候选 API 31/35/36 矩阵亦全部成功。 |
| Debug APK | PASS | JDK 17-like clean 门禁中的 `:app:assembleDebug` 通过。 |
| Release APK | PASS | 当前应用源码候选 `:app:assembleRelease` 成功；本地输出 `app-release.apk`，17,971,235 bytes。测试/文档脚本的后续改动不进入 APK；最终 GitHub Release 仍须由 tag workflow 重建并复验。 |

建议最终命令（以实际 Gradle task 为准）：

```bash
cd native-ui
./gradlew --no-daemon clean \
  :app:testDebugUnitTest \
  :app:lintDebug \
  :app:validateDebugScreenshotTest \
  :app:assembleDebug \
  :app:assembleRelease
```

## 4. 签名、R8 与 APK 安全

| 项目 | 状态 | 待回填证据 |
|---|---|---|
| 稳定发行证书 | PASS | 本地发行证书有效期 2025-12-29 至 2053-05-16；SHA-256 `00:BE:4C:DD:83:78:AA:FB:D7:0E:BC:43:E9:71:52:37:91:CC:07:DE:EA:D6:31:E0:6D:CF:15:5D:EE:EE:38:02`，与 GitHub `release` environment 登记值逐字节一致。未记录别名或密码。 |
| R8 mapping | PASS | `mapping.txt` 95,661,567 bytes；保留为受控构建证据，不作为公开 Release 资产。 |
| R8 seeds/usage/configuration | PASS | `seeds.txt` 768,628 bytes、`usage.txt` 11,072,847 bytes、`configuration.txt` 67,338 bytes。 |
| APK 包身份 | PASS | `com.promenar.nexara.native`、versionCode 2、versionName `0.2-beta`。 |
| 单一签名者/证书匹配 | PASS | 统一 APK 验证器兼容 Build Tools 37 `V2 Signer`，确认唯一签名者且证书与登记 SHA-256 一致。 |
| ZIP/体积/敏感内容扫描 | PASS | `verify-release-apk.py` 对真实 17,971,235-byte APK 验证通过；低于 50 MiB，ZIP 与跨块敏感内容扫描通过。 |
| zipalign | PASS | `zipalign -c -P 16 4` 通过。 |
| SHA-256 | PASS | `d1a26735d25fac02eb282e00f5ec26e9fc76783b87cc55b3d01f209ade5966f1`；生成 `.sha256` 后在文件目录再次 `sha256sum -c` 通过。 |
| Release logcat | PASS | API 35/36 冷启动 crash buffer 均为空；按应用 PID 提取 20/33 行日志，Prompt、Authorization、Bearer、Key、SQL 参数和完整堆栈扫描无命中。 |
| GGUF/llama/ggml 排除 | PASS | 真实签名 APK 扫描通过，未打包 GGUF、llama 或 ggml 制品。 |

## 5. 冷安装与 minified 业务验真

| 场景 | API 35 | API 36 | 证据要求 |
|---|---|---|---|
| 卸载旧包、侧载签名 APK、冷启动、无 crash/ANR | PASS | PASS | 同一 17,971,235-byte APK 在 API 35/36 均完成卸载、安装、设备 `base.apk` 字节回读一致、包身份/版本/证书、launcher、前台进程与 5 秒 crash/ANR 观察；exit=0。 |
| Room 新建、强停、重启读取 | PENDING | PENDING | minified APK 实际数据。 |
| 中英文 onboarding 至首聊 | PENDING | PENDING | 截图 + UI tree/测试报告。 |
| Provider 表单与模型选择 | PENDING | PENDING | 不使用真实 Key 的黑盒流程。 |
| 无 Key 备份 round-trip | PENDING | PENDING | 导出、强停、恢复、no-replay。 |
| 含 Key 加密备份 round-trip | PENDING | PENDING | 仅用脱敏测试凭据，不在日志/截图暴露内容。 |
| DOCX/PDF 解析 | PASS | PASS | API 35/36 `minifiedTest` APK 使用真实 PDF/DOCX fixture 完成分享导入，覆盖 R8/反射边界；证据位于对应 `artifacts/android-minified-blackbox-*` 目录。 |
| RAG 导入/索引/重试 | PASS | PASS | API 35/36 release-equivalent minifiedTest 均使用真实 TXT fixture 完成导入，观察到索引失败、启用 Retry、点击后再次失败并可再次重试；API 35 证据目录为 `artifacts/android-minified-blackbox-api-35-arm64-rag-room-20260715-final/`，exit=0、crash buffer 为空。 |
| 后台/锁屏/旋转/停止/返回 | PENDING | PENDING | 当前任务持续、终态唯一、通知返回准确会话。 |

当前 workflow 的签名 APK smoke 只覆盖校验、冷安装、启动、前台存活与 crash/ANR；在 minified 关键业务流程补齐前，不得把该 smoke 描述为完整发行 E2E。

## 6. UI、视觉与无障碍矩阵

所有截图必须来自最终集成后的当前构建，逐张人工审阅。旧安装包或历史 `/tmp` 截图只能作线索，不能作为本版 PASS 证据。

2026-07-15 已逐张人工审阅当前 36 张截图基线：手机/平板、竖屏/横屏、中文/英文、2.0x 字体、空态/加载/错误/流式/审批/确认框等状态均未发现发行阻断级视觉缺陷；超长模型名在容器内换行，单行可编辑标题在大字体下按输入框语义横向滚动，均未造成关键操作不可达。新增横屏覆盖英文聊天空态、中文生成态、英文文档编辑脏状态和中文 Provider 长文本状态；资源列表相对时间已改为由预览固定时钟驱动，避免基线随真实时间漂移。文档冲突状态在 2.0x 字体下会优先保留复制与重载动作的完整触控目标，并把非关键状态栏让位给本地内容预览；对应预期基线已人工比较后更新。真实 IME 专项已完成，TalkBack 自动语义已闭合；人工听觉与全焦点遍历安排在最终签名发行 APK 真机安装后验收。

| 视口/条件 | 中文 | 英文 | 状态 |
|---|---|---|---|
| 360dp 紧凑手机 | PASS | PASS | Screenshot 基线覆盖双语紧凑布局，人工审阅通过。 |
| Pixel 7 竖屏 | PASS | PASS | 412×892 双语关键页面基线，人工审阅通过。 |
| Pixel 7 横屏 | PASS | PASS | 4 个关键横屏状态基线通过并逐张人工审阅；聊天输入区、文档保存入口、Provider 搜索及批量操作均可达。 |
| 系统字体 2.0x | PASS | PASS | 多个双语关键状态基线覆盖，人工审阅未发现关键控件不可达。 |
| 平板宽度/NavigationRail | PASS | PASS | 840×900 双语基线与 API 设备自适应导航测试通过。 |
| TalkBack 焦点、角色、动态播报 | PENDING | PENDING | API 31/35/36 无障碍 smoke 已通过；API 36 在 TalkBack 服务真实绑定时新增稳定输入名称、Prompt 编辑器名称、保存中 polite 与保存失败 assertive 语义断言，共 7/7。最终签名发行 APK 安装到真机后执行人工听觉与完整焦点遍历。 |
| IME 遮挡、滚动、返回、空/加载/错误态 | PASS | PASS | `ChatImeInteractionTest` 在 API 31/35/36 各 5/5；覆盖长消息尾部避让、键盘动画稳定、主动上滚不被夺回、关闭后 inset 清零与测试隔离。 |

关键页面/状态：欢迎与引导、Agent 首页、聊天空态/生成态/错误态/审批态、Provider、RAG、备份、设置、Prompt 编辑器、通知返回/停止。发现裁切、溢出、低对比、焦点陷阱、低于 48dp 的关键目标、键盘遮挡、无效按钮或不可恢复错误时，门禁为 FAIL。

## 7. 真实 API Smoke

| 项目 | 状态 | 安全要求 |
|---|---|---|
| 多模态模型 | PASS | `MiniMax-M3`：图片输入、有效流式 payload 与完成终态通过。URL/Key 仅由主控经进程环境变量注入，未写入仓库、报告或提交。 |
| 高速文本模型 | PASS | `MiniMax-M2.7-highspeed`：有效文本流与完成终态通过。 |
| 高智力文本模型 | PASS | `deepseek-v4-flash`：有效推理/文本 payload 与完成终态通过。 |
| 中等智力多模态模型 | PASS | `sensenova-6.7-flash-lite`：图片输入与 `delta.reasoning` 流式兼容通过。 |

真实 API 只验证协议兼容、路由、流式终态和多模态输入；Release 仍保持 HTTPS-only，不能为内网测试放宽发行网络策略。

## 8. GitHub 治理与发布

| 项目 | 状态 | 待回填证据 |
|---|---|---|
| 最终提交已推送 | PENDING | 分支、commit SHA、工作树 clean。 |
| tag provenance | PENDING | tag object、目标 commit ancestry、GitHub verification；不允许 lightweight tag。 |
| annotated 降级 | PENDING | 默认拒绝；如签名 tag 不可用，必须有用户明确授权与仓库变量记录。 |
| release environment | PASS | 四项签名 Secret 已存在；公开证书指纹与本地发行证书一致；1 分钟 wait timer 与 custom tag policy 已核验。未读取或记录 Secret 值。 |
| Android CI | PASS | 首次 run `29389720531` 与第二次 run `29398804571` 的失败均已定位并闭环；最终候选 [run 29401903929](https://github.com/Promenar/Nexara/actions/runs/29401903929) 在提交 `10d6761` 上 quality、API 31、API 35、API 36 全部成功。 |
| tag release workflow | PENDING | 远端 run URL、每个 job 结论。 |
| GitHub Release | PENDING | prerelease URL、APK 与 checksum 资产、远端回读哈希一致。 |

## 9. 已知限制确认

| 限制 | 状态 |
|---|---|
| 不兼容 `v0.1-beta` 数据，要求卸载旧版 | 已写入 README 与发行说明 |
| GGUF 不属于稳定能力，Release 不打包 | 已写入 README 与发行说明；本地稳定证书签名 APK 真实扫描 PASS，tag workflow 产物仍须复验 |
| 后台生成不支持设备重启续传、多会话并行、无人值守队列或定时任务 | 已写入 README 与发行说明 |
| Release Provider/WebDAV 仅 HTTPS | 已写入 README 与发行说明 |
| 仅深色主题为正式主题 | 已写入发行说明 |

## 10. 最终签字区

- 最终结论：`NO-GO / PENDING`
- 阻断项：GitHub 可验证 tag 签名身份、签名候选真机 TalkBack 人工听觉/全焦点遍历与核心业务体验、tag release workflow 与 GitHub Release 尚未完成。
- 放行人：PENDING
- 放行时间：PENDING
- 最终 commit：PENDING
- Release URL：PENDING
- APK SHA-256：PENDING
