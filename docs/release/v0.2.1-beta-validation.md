# Nexara v0.2.1-beta 发行验证记录

> 状态：**PENDING**
> 更新日期：2026-08-25
> 当前工作分支：`B-native-refactor`
> 当前应用候选：versionCode `3` / versionName `0.2.1-beta`
> 物理真机人工验收：PENDING
> GitHub 发布动作：NOT-AUTHORIZED
> 当前本地 APK 冷安装：PENDING
> 同源历史候选冷安装：PENDING
> 旧版覆盖升级数据继承：PENDING

本文件只记录当前工作树和最终签名 APK 的直接证据。历史 `v0.2-beta` 的测试、编译成功、测试源码存在、子 Agent 报告或 Debug 安装均不能替代本候选的最终结果。

## 放行矩阵

| 门禁 | 当前状态 | 证据 |
|---|---|---|
| Task11 Critical / Important 独立复审 | PENDING | 等待工具取消、MCP SSE、未知 Provider 可恢复入口及 RAG/KG 最终复审。 |
| 全量 JVM Unit | PENDING | 待最终共享工作树执行 clean、无缓存全量测试并回填精确计数。 |
| Lint | PENDING | 待执行；要求 0 Error / 0 Fatal。 |
| Screenshot validation | PENDING | 待更新受影响 reference、逐张检查 actual 并全量验证。 |
| AndroidTest 编译与迁移设备测试 | PENDING | v1→v5 迁移测试已新增，待受控 API 35 设备执行。 |
| Android device core E2E | PENDING | 待最终 Debug/deviceTest 候选执行。 |
| minified blackbox | PENDING | 待最终 release-equivalent 候选执行。 |
| Metro TUI | PENDING | 待最终 Node 测试执行。 |
| 正式签名 R8 APK | PENDING | 待安全签名构建、R8 文件、zipalign、包身份、证书和 SHA-256 验真。 |
| 当前 APK 冷安装 | PENDING | 必须在专用受控模拟器卸载目标包后安装最终同一 APK。 |
| v0.1 历史 APK 冷安装 | PENDING | 必须验证冻结 SHA-256 的公开同签名 `v0.1-beta`。 |
| v0.1→v0.2.1 覆盖升级 | PENDING | 写入数据库、偏好和物理文件哨兵后，只允许 `adb install -r`，禁止卸载或清数据回退。 |
| 物理真机人工体验 | PENDING | 由用户在交付 APK 上验证，不由自动化冒充。 |
| GitHub tag / Release | NOT-AUTHORIZED | 本轮只授权完成 APK，不创建 tag、Release 或公开资产。 |

## 数据继承哨兵

覆盖升级门禁至少回读以下旧版数据：

- Room schema 从 v1 前进到 v5；
- Agent、Session 和 Message 行；
- Provider 协议、地址、模型与名称偏好；
- 用户名偏好；
- 工作区 root/file 数据库行及对应物理文件内容；
- package `firstInstallTime` 在覆盖安装前后保持不变；
- 新旧 APK 包名一致、versionCode 严格递增、签名证书 SHA-256 完全一致。

## 尚未授权或不属于自动化 PASS 的项目

- 未调用付费真实 Provider；本轮协议结论来自本地 golden、MockEngine 和确定性测试。
- 未授权创建或推送 tag、GitHub Release、PR 或公开 APK。
- 物理真机 TalkBack、完整核心业务体验、OEM 系统栏和 IME 仍由用户验收。

## 最终回填

- 最终 commit：PENDING
- APK 绝对路径：PENDING
- APK 字节数：PENDING
- APK SHA-256：PENDING
- signer SHA-256：PENDING
- 冷安装设备/API：PENDING
- 覆盖升级设备/API：PENDING
