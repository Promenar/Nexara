# Nexara v0.2.1-beta 发行验证记录

> 状态：**LOCAL-DETERMINISTIC-AND-APK-PASS / GATEWAY-PARTIAL / HUMAN-PENDING**
> 更新日期：2026-09-22
> 工作分支：`B-native-refactor`；审计基线：`6e3d341707dd548efb78b67b391778fc69aef8ae`
> 物理真机人工验收：PENDING
> GitHub 发布动作：NOT-AUTHORIZED

## 当前候选与来源

用户批准本机 Android 模拟器、指定无密钥网关有界测试及修复计划；PDEC 契约可执行、无漂移。完整范围见[需求总账](../audit/20260922-requirements-acceptance.md)和[十一项工作包计划](../superpowers/plans/2026-09-22-audit-completion-repair.md)。以下是本机证据，不宣称远端当前提交 CI 已通过。

基线 CI `32946466042` 实际为2571项、2失败、14跳过，设备E2E跳过；本轮不继承历史账本的自动化全绿。公开 v0.2-beta 的真实 schema 为v2，三段升级形成v17+v2；内部v17+v5另有夹具，不能互相替代。

| 制品字段 | 当前值 |
| --- | --- |
| APK | `artifacts/audit-20260922/release/nexara-v0.2.1-beta-audit-final.apk` |
| SHA-256 | `aa362031bc9b731f061acb4760ee8160678c517f6d465f3e71f8db8dbd135b66` |
| 字节数 | 20,255,320 |
| 包/版本 | `com.promenar.nexara.native` / code3 / `0.2.1-beta` |
| 唯一发行证书SHA-256 | `00be4cdd8378aafbd70ebc43e971523791cc07deead631e06dcf155deeee3802` |
| 源码输入 | 1099个native-ui文件；聚合SHA-256 `77a6d2b32585937cdba8887c8d6962090e011286b31e3abe32c3387b4c54ca80` |
| 绑定清单 | `artifacts/audit-20260922-final-source-inputs.json`、`artifacts/audit-20260922/release/delivery-manifest.json` |

源码清单按路径排序，聚合内容为每行“文件SHA-256、两个空格、仓库相对路径、LF”的UTF-8字节。构建发生于已冻结、尚未提交的工作树；清单记录全部输入并在构建后重验，最终交付manifest另记录Git提交与native-ui tree。不能把基线HEAD当作本制品源码。

## 本机质量与设备矩阵

| 门禁 | 结果与范围 | 原始证据（均位于artifacts，不纳管） |
| --- | --- | --- |
| 全量JVM | 2715项，0失败/错误，24跳过，2691实际执行通过 | `audit-20260922-final-quality.log`、`audit-20260922-final-quality/jvm-xml/` |
| 跳过说明 | 13项既有协议条件、1项既有真实服务opt-in、10项新增本地网关opt-in；真实调用另表记账 | 最终XML的skipped节点，不扩展skip掩盖失败 |
| 截图 | 100/100，零失败/跳过 | `audit-20260922-final-quality/screenshot-results/` |
| Lint/Debug构建 | 0 Error/Fatal；527 Warning、22 Hint；构建成功 | 最终质量log及lint.xml；未声称警告清零 |
| API31 minimum | 14正常阶段、76断言通过 | `audit-20260922-device-api31-minimum1/` |
| API35 full | 36正常阶段、116断言、2预期进程终止阶段通过 | `audit-20260922-device-api35-full3/` |
| API36最终full | 36正常阶段、116断言、2预期进程终止阶段通过 | `audit-20260922-device-api36-final/` |
| 编辑器最终功能 | API31/35/36各34项通过；非末尾选区旧实现RED、实际横屏/IME新实现GREEN | `audit-20260922-editor-selection-physical-red.txt`、`audit-20260922-editor-interaction-physical-green.txt`、`audit-20260922-editor-interaction-final-api31.txt`/`api35.txt` |
| 宿主脚本 | Python93、Node49通过；Release workflow行为测试35通过；设备脚本合同通过 | 对应audit日志；脚本修改有正负解析回归 |
| 模型目录 | 421条刷新候选14项通过后精确恢复原资产 | `audit-20260922-refreshed-catalog-check/`；未采用未经语义复核的新目录 |
| 独立复核 | 身份/发布竞态、双库/附件/路径别名/备份及编辑器增量分别审阅；发现项修复后定向和集成验收 | 原生Agent审阅与HLG记录；主控复核关键源码、diff、RED/GREEN和设备结果 |

API31/35核心矩阵对应最终编辑器改动之前的集成源码。1099文件清单对比只相差DocEditorScreen及其两个设备测试文件；数据、身份、恢复、导入、搜索、协议、备份与核心测试脚本字节未变。其对应领域证据可以复用，不能表述为最终APK在两档设备重新跑过116项。API36核心矩阵与三个API的编辑器专项在最终源码执行；正式APK采用下面独立的同哈希门禁。

## 正式签名APK门禁

| 门禁 | 当前结果 | 证据 |
| --- | --- | --- |
| 签名、版本、体积、敏感内容、本地推理制品排除 | PASS | `audit-20260922-final-apk-verification.txt` |
| R8四类输出、ZIP/64位ELF对齐 | PASS | release下final-mapping/seeds/usage/configuration；`audit-20260922-final-release-elf-alignment.json` |
| API36同哈希冷安装 | PASS，ADB COLD显示205ms | `audit-20260922-release-final-api36/`；设备base.apk回拉逐字节一致 |
| API36混淆黑盒 | PASS：冷启动、PDF/DOCX分享、文本索引失败重试 | `audit-20260922-minified-final-api36/` |
| API35同哈希冷安装 | PASS，ADB COLD显示196ms | `audit-20260922-release-final-api35/` |
| 公开v0.1→最终候选 | PASS，WAL/业务/文件/偏好及安装时间保持 | `audit-20260922-release-upgrade-final-v01.log` |
| 公开v0.1→v0.2→最终候选 | PASS，两侧实际入口、活动文件与来源归档回读 | `audit-20260922-dual-release-final/` |
| API31同哈希冷安装 | PASS，ADB COLD显示179ms | `audit-20260922-release-final-api31/` |

ADB COLD数值仅为启动显示计时，不代表首个业务页面完整就绪或整个PRD冷启动指标通过。三档设备base.apk均回拉与交付APK逐字节一致。单库升级firstInstallTime为2026-09-22 15:21:45；三段升级为15:21:53，事务`41bc5203-4037-4835-b1f2-362f9befa731`。专用AVD和本任务adb reverse已停止/移除，证据与AVD数据保留。

公开历史APK：v0.1 SHA `0dfe33b280af97b94c27203b9e66691ff8e41af237777a93d21d2b27907b9291`；v0.2 SHA `59d641b6b8f04a15bc9f4ce064a7a116df3def4efabf8231feb408612246fe9a`。覆盖安装要求同包、同一证书、版本1→2→3严格递增；只在专用API35合成数据环境使用root夹具。

双库验收包含用户实际点击“保留两份并恢复”，两侧Agent→Session→Message可达，旧data URL附件点击后缓存内容哈希一致，当前完整上下文文档可见，v18 identity/FK/receipt与冲突ID关联正确，活动工作区文件和归档均按字节/hash回读，来源及firstInstallTime保持。源库保全本身不能替代这些业务断言。

早期签名候选SHA `4677d3ee3c800918105cd2ba9a2e4c8682e5b19cc67da57c596094f66cfac055`及其安装/升级证据保留；它不含最终编辑器Row修复，不作为当前交付制品。夹具失败、中断和诊断记录同样保留。

## 性能测量及限制

固定专用API36 ARM64、host GPU、无窗口/无快照、动画0；性能期间关闭专用AVD的WiFi/data，结束后恢复。与功能回归、签名APK门禁分别记账，测量变体为deviceTest。

| 场景 | 结果 | 边界 |
| --- | --- | --- |
| 500模型列表 | 两个有效批次最差p95=35ms、max=69ms、PSS+3341KiB；90份histogram独立复算一致 | `audit-20260922-provider-performance-api36-3/` run1/run3有效；run2系统包事件无效且保留 |
| 1000/2000条会话消息 | 两项通过，5轮最大分别11.91/55.20ms；每次一条消息SELECT，核对数量/首尾/Unicode | 重开Room连接+反序列化；不等于OS冷缓存、ChatStore或UI首屏 |
| 编辑器6场景 | 最终无后台分析复验6/6，最差p95=36ms、max=68ms、PSS+992KiB | `audit-20260922-editor-performance-api36-final-clean/`；3次预热、5次测量，原50/150ms、64MiB、波动0.20门槛 |

编辑器先修复旧测试dispatcher导致的跨线程重组，使用项目已有Compose v2规则；生产修复统一Row内共享面板调用位置，保留选区。早期v2三项超限、Row后行数边界超限及trace诊断均保留。测试包曾请求speed编译，但系统实际降为verify，应用为run-from-apk；不声称AOT或已证明性能改善因果。此前6/6批次可能与trace导出重叠，最终另行取得无并行构建/trace分析的6/6证据。通过只适用于记录的设备和编译条件，未证明首次安装路径普遍稳定。

## 真实网关与未关闭指标

只向用户授权的回环网关发送有界合成数据；正式版HTTPS策略没有放宽。

| 模型 | 文本 | 两轮工具 |
| --- | --- | --- |
| newapi/deepseek-v4-flash | PASS | PASS |
| newapi/sensenova-6.8-flash-lite | PASS | PASS |
| openai-chatgpt/gpt-5.6-luna | PASS | PASS |
| newapi/gemini-3.8-flash | PASS | FAIL：工具流出现结构化错误事件 |
| newapi/MiniMax-M3 | FAIL：空响应 | FAIL：无有效tool_calls |

设备端五模型文本为4通过、1失败；JVM工具夹具验证真实参数与第二轮回传。Generic/OpenAI合法null字段已修复，错误非空类型、错误事件、缺失终态与空响应仍失败关闭。模型可见性和HTTP200不能记为成功；未修改网关服务，也不把兼容网关结果外推到原生OpenAI/Responses/Anthropic/Vertex、视觉、Embedding/Rerank。

局部性能门禁不关闭PRD全局16ms、TTFT<500ms、端到端消息首屏、Top-5召回>80%、覆盖率>80%、崩溃率<0.1%及警告清零。16KiB zipalign与64位PT_LOAD静态检查通过，模拟器实测PAGE_SIZE=4096，16KiB运行未验；RELRO公式告警经布局复核未发现取整覆盖可写数据，不将其写为已复现崩溃。物理TalkBack、完整焦点遍历、OEM/IME体验另验。GGUF/本地推理、自动备份调度、重启续传、多会话并行、无人值守队列保持延期；公开发布未授权。

## 历史候选记录：2026-08-26

以下保留原候选的审计记录，包括当时的状态声明、版本、APK 哈希和验收范围；不能替代当前修复候选的证据。

> 状态：**AUTOMATION-PASS / HUMAN-PENDING**
> 更新日期：2026-08-26
> 当前工作分支：`B-native-refactor`
> 当前应用候选：versionCode `3` / versionName `0.2.1-beta`
> 物理真机人工验收：PENDING
> GitHub 发布动作：NOT-AUTHORIZED
> 当前本地 APK 冷安装：NOT-RUN（当前无连接设备；上一签名候选 API 35 PASS）
> 同源历史候选冷安装：PASS（公开 `v0.1-beta`，API 35）
> 旧版覆盖升级数据继承：HISTORICAL-PASS / CURRENT-NOT-RUN（上一签名候选 API 35 PASS）
> v0.1→v0.2→当前三段双库升级：BLOCKED（拒绝静默选库，待双库恢复工具）

本文件只记录当前工作树和最终签名 APK 的直接证据。历史 `v0.2-beta` 的测试、编译成功、测试源码存在、子 Agent 报告或 Debug 安装均不能替代本候选的最终结果。

### 放行矩阵

| 门禁 | 当前状态 | 证据 |
|---|---|---|
| Critical / Important 独立复审 | HISTORICAL-PASS / CURRENT-NOT-RUN | 上一候选独立复审 GO；本轮未调用独立 Agent，不把历史审阅冒充当前增量审阅。 |
| 全量 JVM Unit | PASS | `:app:testDebugUnitTest`：2571 项，0 failure/error，14 skip；新增无描边分组面、记忆设置语义分组与 Agent 列表无装饰分割线合同。 |
| Lint | PASS | `:app:lintDebug`：0 Error/Fatal。 |
| Screenshot validation | PASS | `:app:validateDebugScreenshotTest`：100/100；已人工抽查记忆设置深色大字、Agent 首页普通/2.0x、外观页与受共享分组面影响的设置基线，底部三按钮导航结构与尺寸不变。 |
| AndroidTest 编译与迁移设备测试 | PASS | AndroidTest 编译通过；API 35 数据库迁移 4/4，FTS4 验证副本/WAL+journal 零写入与真实 bind-mount 别名 3/3。 |
| Android device core E2E | PASS | API 35 full scope 全部通过，覆盖通知、聊天、引导、无障碍、自适应导航、备份恢复、进程重建、文档解析与前台服务。 |
| 头像裁剪设备闭环 | PASS | API 35 从 900×600 源图进入真实裁剪 Activity；中文“完成”可按坐标点击，顶栏避让状态栏，底部旧控件隐藏，输出为不大于 512px 的正方形。Agent 与用户入口复用同一合同。 |
| Agent 首页头像与 MD3 列表 | PASS | API 35 `AgentHubScreenContentTest` 9/9；自定义路径命中真实图片分支。JVM 回归确认从自定义图片选择内置头像会立即以 `avatarPath=null` 和新 `icon` 提交 Repository，不依赖页面延迟保存。普通与中文 2.0x 截图均以 88dp 连续列表行、56dp 圆形头像渲染；底部导航组件零 diff。 |
| 会话错误单一呈现 | PASS | 结构化失败状态区分 `INLINE_MESSAGE` 与 `OVERLAY`：前者只保留可删除错误气泡，后者继续使用顶层 Snackbar；GenerationPresentationStore、ChatViewModel、ChatRoute 契约回归通过。 |
| minified blackbox | HISTORICAL-PASS / CURRENT-NOT-RUN | 上一候选完成冷启动、PDF/DOCX 分享导入及文本索引失败重试；当前哈希因无连接设备未重跑。 |
| Metro TUI | PASS | Node 49/49；宿主脚本单测 93/93，Release workflow 33/33，timeout 8/8，PNG 4/4，R8 规则 5/5。 |
| 正式签名 R8 APK | PASS | 从 `clean :app:assembleRelease` 构建；包身份、版本、单一 signer、登记证书、敏感内容、本地推理制品排除、ZIP、R8 mapping/seeds/usage/configuration、16 KiB zipalign 与 checksum 验真通过。 |
| 当前 APK 冷安装 | NOT-RUN | 本轮 `adb devices -l` 无连接设备；不沿用上一 APK 的安装结果冒充当前哈希验证。 |
| v0.1 历史 APK 冷安装 | PASS | 冻结公开 APK SHA-256 `0dfe33b280af97b94c27203b9e66691ff8e41af237777a93d21d2b27907b9291`，启动后实测 Room v17。 |
| v0.1→v0.2.1 覆盖升级 | HISTORICAL-PASS / CURRENT-NOT-RUN | 上一同版本签名候选只经 `adb install -r` 覆盖后，完整哨兵、v18 identity 与 `firstInstallTime` 回读均通过；本轮最终哈希因无连接设备未重跑，不冒充当前 APK 的直接证据。 |
| v0.1→v0.2→v0.2.1 三段升级 | BLOCKED | 该链会同时留下公开 v17 与当前 v5 两库；本版已改为写入前 fail-close，尚未提供可证明无损的双库合并。保留安装与完整设备备份，等待维护者双库恢复工具。 |
| 物理真机人工体验 | PENDING | 由用户在交付 APK 上验证，不由自动化冒充。 |
| GitHub tag / Release | NOT-AUTHORIZED | 本轮只授权完成 APK，不创建 tag、Release 或公开资产。 |

### 数据继承哨兵

覆盖升级门禁至少回读以下旧版数据：

- 公开 `nexara.db` 的实测 Room v17 identity 与列合同校验通过，WAL 完整 checkpoint 后接管到 `nexara_v2.db` 并迁移到 v18；当前库同时保留 v1→v5→v18 前进路径；
- Agent、Session 和 Message 行；
- 旧消息附件 JSON、向量、上下文摘要、任务树与 RAG/工作区根关系；
- Provider 协议、地址、模型与名称偏好；
- 用户名偏好；
- 工作区 root/file 数据库行及对应物理文件内容；
- 精确旧路径 `files/workspaces/<sessionId>` 在真实启动后建立根身份标记且文件仍可读；
- package `firstInstallTime` 在覆盖安装前后保持不变；
- 新旧 APK 包名一致、versionCode 严格递增、签名证书 SHA-256 完全一致。

双库歧义门禁：当公开 `nexara.db` v17 与 `nexara_v2.db` v5 同时存在时，两个库都会先按精确 identity 与关键列只读验真，随后在任何接管、迁移或合并写入前失败；两库均原样保留。该门禁防止静默丢数据，但不等于三段升级已经兼容。

### 尚未授权或不属于自动化 PASS 的项目

- 未调用付费真实 Provider；本轮协议结论来自本地 golden、MockEngine 和确定性测试。
- 未授权创建或推送 tag、GitHub Release、PR 或公开 APK。
- 物理真机 TalkBack、完整核心业务体验、OEM 系统栏和 IME 仍由用户验收。
- `v0.1→v0.2→当前版` 双库合并/显式恢复仍未实现；遇到该链必须保留安装与完整设备备份，不得通过卸载、清数据或只保留任一数据库规避门禁。

### 最终回填

- APK 对应生产代码 commit：`b89673b7956e3dea764b5b9c251af504d8687ad4`
- APK 绝对路径：`/Users/promenar/Codex/Nexara/artifacts/Nexara-0.2.1-beta-20260826-clean-groups-release.apk`
- APK 字节数：`18,909,055`
- APK SHA-256：`79d6b7c71b903fe783e4bd4d1625b6a3794154cfb292731c0efbd6f9759f8c5e`
- signer SHA-256：`00be4cdd8378aafbd70ebc43e971523791cc07deead631e06dcf155deeee3802`
- 冷安装设备/API：当前 APK NOT-RUN；上一同版本签名候选为 `emulator-5554` / API 35 / arm64-v8a
- 覆盖升级设备/API：当前 APK NOT-RUN；上一同版本签名候选为 `emulator-5554` / API 35 / arm64-v8a，`UPGRADE_SMOKE=PASS`
